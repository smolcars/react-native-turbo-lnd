// Note to react-native-turbo-lnd contributors:
// This file is automatically generated by the protoc plugin inside the
// protoc-generator folder.
// Any changes to this file should be made there instead.
/* eslint-disable */
import "./setup-text-encoding";

let TurboLnd: any;
if ((globalThis as any)["fakelnd"] || typeof jest !== 'undefined') {
  TurboLnd = require('./mocks/index').default;
} else {
  TurboLnd = require('./core/NativeTurboLnd').default;
}
import { type OnResponseCallback, type OnErrorCallback, type UnsubscribeFromStream } from "./core/NativeTurboLnd";

import { create, toBinary, fromBinary, type MessageInitShape } from "@bufbuild/protobuf";

import * as lnrpc from "./proto/lightning_pb";
// import * as walletunlocker from "./proto/walletunlocker_pb";
// import * as state from "./proto/stateservice_pb";
import * as autopilotrpc from "./proto/autopilotrpc/autopilot_pb";
// import * as chainrpc from "./proto/chainrpc/chainkit_pb";
import * as chainrpc from "./proto/chainrpc/chainnotifier_pb";
// import * as dev from "./proto/devrpc/dev_pb";
import * as invoicesrpc from "./proto/invoicesrpc/invoices_pb";
// import * as versionresponse from "./proto/lnclipb/lncli_pb";
import * as neutrinorpc from "./proto/neutrinorpc/neutrino_pb";
import * as peersrpc from "./proto/peersrpc/peers_pb";
import * as routerrpc from "./proto/routerrpc/router_pb";
import * as signrpc from "./proto/signrpc/signer_pb";
import * as verrpc from "./proto/verrpc/verrpc_pb";
import * as walletrpc from "./proto/walletrpc/walletkit_pb";
import * as watchtowerrpc from "./proto/watchtowerrpc/watchtower_pb";
import * as wtclientrpc from "./proto/wtclientrpc/wtclient_pb";

/**
 *
 * Starts up lnd.
 * You need to provide path to the app's local dir to lnd via `--lnddir` arg.
 * Use `subscribeState` to know when lnd is ready for wallet unlock/creation.
 *
 */
export const start = TurboLnd.start;

/**
   *
   * WalletBalance returns total unspent outputs(confirmed and unconfirmed), all
   * confirmed unspent outputs and all unconfirmed unspent outputs under control
   * of the wallet.
   *
   * @param [WalletBalanceRequest]
   * @returns [WalletBalanceResponse]
   *
   */
export async function walletBalance(
  request: MessageInitShape<typeof lnrpc.WalletBalanceRequestSchema>
): Promise<lnrpc.WalletBalanceResponse> {
  const message = create(
    lnrpc.WalletBalanceRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.walletBalance(
    toBinary(lnrpc.WalletBalanceRequestSchema, message)
  );
  const response = fromBinary(
    lnrpc.WalletBalanceResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * ChannelBalance returns a report on the total funds across all open channels,
   * categorized in local/remote, pending local/remote and unsettled local/remote
   * balances.
   *
   * @param [ChannelBalanceRequest]
   * @returns [ChannelBalanceResponse]
   *
   */
export async function channelBalance(
  request: MessageInitShape<typeof lnrpc.ChannelBalanceRequestSchema>
): Promise<lnrpc.ChannelBalanceResponse> {
  const message = create(
    lnrpc.ChannelBalanceRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.channelBalance(
    toBinary(lnrpc.ChannelBalanceRequestSchema, message)
  );
  const response = fromBinary(
    lnrpc.ChannelBalanceResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * GetTransactions returns a list describing all the known transactions
   * relevant to the wallet.
   *
   * @param [GetTransactionsRequest]
   * @returns [TransactionDetails]
   *
   */
export async function getTransactions(
  request: MessageInitShape<typeof lnrpc.GetTransactionsRequestSchema>
): Promise<lnrpc.TransactionDetails> {
  const message = create(
    lnrpc.GetTransactionsRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.getTransactions(
    toBinary(lnrpc.GetTransactionsRequestSchema, message)
  );
  const response = fromBinary(
    lnrpc.TransactionDetailsSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * EstimateFee attempts to query the internal fee estimator of the wallet to
   * determine the fee (in sat/kw) to attach to a transaction in order to
   * achieve the confirmation target.
   *
   * @param [EstimateFeeRequest]
   * @returns [EstimateFeeResponse]
   *
   */
export async function estimateFee(
  request: MessageInitShape<typeof lnrpc.EstimateFeeRequestSchema>
): Promise<lnrpc.EstimateFeeResponse> {
  const message = create(
    lnrpc.EstimateFeeRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.estimateFee(
    toBinary(lnrpc.EstimateFeeRequestSchema, message)
  );
  const response = fromBinary(
    lnrpc.EstimateFeeResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * SendCoins executes a request to send coins to a particular address. Unlike
   * SendMany, this RPC call only allows creating a single output at a time. If
   * neither target_conf, or sat_per_vbyte are set, then the internal wallet will
   * consult its fee model to determine a fee for the default confirmation
   * target.
   *
   * @param [SendCoinsRequest]
   * @returns [SendCoinsResponse]
   *
   */
export async function sendCoins(
  request: MessageInitShape<typeof lnrpc.SendCoinsRequestSchema>
): Promise<lnrpc.SendCoinsResponse> {
  const message = create(
    lnrpc.SendCoinsRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.sendCoins(
    toBinary(lnrpc.SendCoinsRequestSchema, message)
  );
  const response = fromBinary(
    lnrpc.SendCoinsResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * ListUnspent returns a list of all utxos spendable by the wallet with a
   * number of confirmations between the specified minimum and maximum. By
   * default, all utxos are listed. To list only the unconfirmed utxos, set
   * the unconfirmed_only to true.
   *
   * @param [ListUnspentRequest]
   * @returns [ListUnspentResponse]
   *
   */
export async function listUnspent(
  request: MessageInitShape<typeof lnrpc.ListUnspentRequestSchema>
): Promise<lnrpc.ListUnspentResponse> {
  const message = create(
    lnrpc.ListUnspentRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.listUnspent(
    toBinary(lnrpc.ListUnspentRequestSchema, message)
  );
  const response = fromBinary(
    lnrpc.ListUnspentResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * SubscribeTransactions creates a uni-directional stream from the server to
   * the client in which any newly discovered transactions relevant to the
   * wallet are sent over.
   *
   * @param [GetTransactionsRequest]
   * @returns [Transaction]
   *
   */
export function subscribeTransactions(
  request: MessageInitShape<typeof lnrpc.GetTransactionsRequestSchema>,
  onResponse: (response: lnrpc.Transaction) => void,
  onError: (error: string) => void
): UnsubscribeFromStream {
  const message = create(
    lnrpc.GetTransactionsRequestSchema,
    request
  );
  const requestBytes = toBinary(
    lnrpc.GetTransactionsRequestSchema,
    message
  );

  const onResponseWrapper: OnResponseCallback = (responseBytes) => {
    onResponse(fromBinary(
      lnrpc.TransactionSchema,
      responseBytes
    ));
  }
  const onErrorWrapper: OnErrorCallback = (error: string) => onError(error);

  return TurboLnd.subscribeTransactions(requestBytes, onResponseWrapper, onErrorWrapper);
}


/**
   *
   * SendMany handles a request for a transaction that creates multiple specified
   * outputs in parallel. If neither target_conf, or sat_per_vbyte are set, then
   * the internal wallet will consult its fee model to determine a fee for the
   * default confirmation target.
   *
   * @param [SendManyRequest]
   * @returns [SendManyResponse]
   *
   */
export async function sendMany(
  request: MessageInitShape<typeof lnrpc.SendManyRequestSchema>
): Promise<lnrpc.SendManyResponse> {
  const message = create(
    lnrpc.SendManyRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.sendMany(
    toBinary(lnrpc.SendManyRequestSchema, message)
  );
  const response = fromBinary(
    lnrpc.SendManyResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * NewAddress creates a new address under control of the local wallet.
   *
   * @param [NewAddressRequest]
   * @returns [NewAddressResponse]
   *
   */
export async function newAddress(
  request: MessageInitShape<typeof lnrpc.NewAddressRequestSchema>
): Promise<lnrpc.NewAddressResponse> {
  const message = create(
    lnrpc.NewAddressRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.newAddress(
    toBinary(lnrpc.NewAddressRequestSchema, message)
  );
  const response = fromBinary(
    lnrpc.NewAddressResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * SignMessage signs a message with the key specified in the key locator. The
   * returned signature is fixed-size LN wire format encoded.
   *
   * The main difference to SignMessage in the main RPC is that a specific key is
   * used to sign the message instead of the node identity private key.
   *
   * @param [SignMessageRequest]
   * @returns [SignMessageResponse]
   *
   */
export async function signMessage(
  request: MessageInitShape<typeof lnrpc.SignMessageRequestSchema>
): Promise<lnrpc.SignMessageResponse> {
  const message = create(
    lnrpc.SignMessageRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.signMessage(
    toBinary(lnrpc.SignMessageRequestSchema, message)
  );
  const response = fromBinary(
    lnrpc.SignMessageResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * VerifyMessage verifies a signature over a message using the public key
   * provided. The signature must be fixed-size LN wire format encoded.
   *
   * The main difference to VerifyMessage in the main RPC is that the public key
   * used to sign the message does not have to be a node known to the network.
   *
   * @param [VerifyMessageRequest]
   * @returns [VerifyMessageResponse]
   *
   */
export async function verifyMessage(
  request: MessageInitShape<typeof lnrpc.VerifyMessageRequestSchema>
): Promise<lnrpc.VerifyMessageResponse> {
  const message = create(
    lnrpc.VerifyMessageRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.verifyMessage(
    toBinary(lnrpc.VerifyMessageRequestSchema, message)
  );
  const response = fromBinary(
    lnrpc.VerifyMessageResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * ConnectPeer attempts to establish a connection to a remote peer. This is at
   * the networking level, and is used for communication between nodes. This is
   * distinct from establishing a channel with a peer.
   *
   * @param [ConnectPeerRequest]
   * @returns [ConnectPeerResponse]
   *
   */
export async function connectPeer(
  request: MessageInitShape<typeof lnrpc.ConnectPeerRequestSchema>
): Promise<lnrpc.ConnectPeerResponse> {
  const message = create(
    lnrpc.ConnectPeerRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.connectPeer(
    toBinary(lnrpc.ConnectPeerRequestSchema, message)
  );
  const response = fromBinary(
    lnrpc.ConnectPeerResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * DisconnectPeer disconnects a peer by target address. Both outbound and
   * inbound nodes will be searched for the target node. An error message will
   * be returned if the peer was not found.
   *
   * @param [DisconnectPeerRequest]
   * @returns [DisconnectPeerResponse]
   *
   */
export async function disconnectPeer(
  request: MessageInitShape<typeof lnrpc.DisconnectPeerRequestSchema>
): Promise<lnrpc.DisconnectPeerResponse> {
  const message = create(
    lnrpc.DisconnectPeerRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.disconnectPeer(
    toBinary(lnrpc.DisconnectPeerRequestSchema, message)
  );
  const response = fromBinary(
    lnrpc.DisconnectPeerResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * ListPeers returns a verbose listing of all currently active peers.
   *
   * @param [ListPeersRequest]
   * @returns [ListPeersResponse]
   *
   */
export async function listPeers(
  request: MessageInitShape<typeof lnrpc.ListPeersRequestSchema>
): Promise<lnrpc.ListPeersResponse> {
  const message = create(
    lnrpc.ListPeersRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.listPeers(
    toBinary(lnrpc.ListPeersRequestSchema, message)
  );
  const response = fromBinary(
    lnrpc.ListPeersResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * SubscribePeerEvents creates a uni-directional stream from the server to
   * the client in which any events relevant to the state of peers are sent
   * over. Events include peers going online and offline.
   *
   * @param [PeerEventSubscription]
   * @returns [PeerEvent]
   *
   */
export function subscribePeerEvents(
  request: MessageInitShape<typeof lnrpc.PeerEventSubscriptionSchema>,
  onResponse: (response: lnrpc.PeerEvent) => void,
  onError: (error: string) => void
): UnsubscribeFromStream {
  const message = create(
    lnrpc.PeerEventSubscriptionSchema,
    request
  );
  const requestBytes = toBinary(
    lnrpc.PeerEventSubscriptionSchema,
    message
  );

  const onResponseWrapper: OnResponseCallback = (responseBytes) => {
    onResponse(fromBinary(
      lnrpc.PeerEventSchema,
      responseBytes
    ));
  }
  const onErrorWrapper: OnErrorCallback = (error: string) => onError(error);

  return TurboLnd.subscribePeerEvents(requestBytes, onResponseWrapper, onErrorWrapper);
}


/**
   *
   * GetInfo returns general information concerning the companion watchtower
   * including its public key and URIs where the server is currently
   * listening for clients.
   *
   * @param [GetInfoRequest]
   * @returns [GetInfoResponse]
   *
   */
export async function getInfo(
  request: MessageInitShape<typeof lnrpc.GetInfoRequestSchema>
): Promise<lnrpc.GetInfoResponse> {
  const message = create(
    lnrpc.GetInfoRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.getInfo(
    toBinary(lnrpc.GetInfoRequestSchema, message)
  );
  const response = fromBinary(
    lnrpc.GetInfoResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * GetDebugInfo returns debug information concerning the state of the daemon
   * and its subsystems. This includes the full configuration and the latest log
   * entries from the log file.
   *
   * @param [GetDebugInfoRequest]
   * @returns [GetDebugInfoResponse]
   *
   */
export async function getDebugInfo(
  request: MessageInitShape<typeof lnrpc.GetDebugInfoRequestSchema>
): Promise<lnrpc.GetDebugInfoResponse> {
  const message = create(
    lnrpc.GetDebugInfoRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.getDebugInfo(
    toBinary(lnrpc.GetDebugInfoRequestSchema, message)
  );
  const response = fromBinary(
    lnrpc.GetDebugInfoResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * GetRecoveryInfo returns information concerning the recovery mode including
   * whether it's in a recovery mode, whether the recovery is finished, and the
   * progress made so far.
   *
   * @param [GetRecoveryInfoRequest]
   * @returns [GetRecoveryInfoResponse]
   *
   */
export async function getRecoveryInfo(
  request: MessageInitShape<typeof lnrpc.GetRecoveryInfoRequestSchema>
): Promise<lnrpc.GetRecoveryInfoResponse> {
  const message = create(
    lnrpc.GetRecoveryInfoRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.getRecoveryInfo(
    toBinary(lnrpc.GetRecoveryInfoRequestSchema, message)
  );
  const response = fromBinary(
    lnrpc.GetRecoveryInfoResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * PendingChannels returns a list of all the channels that are currently
   * considered "pending". A channel is pending if it has finished the funding
   * workflow and is waiting for confirmations for the funding txn, or is in the
   * process of closure, either initiated cooperatively or non-cooperatively.
   *
   * @param [PendingChannelsRequest]
   * @returns [PendingChannelsResponse]
   *
   */
export async function pendingChannels(
  request: MessageInitShape<typeof lnrpc.PendingChannelsRequestSchema>
): Promise<lnrpc.PendingChannelsResponse> {
  const message = create(
    lnrpc.PendingChannelsRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.pendingChannels(
    toBinary(lnrpc.PendingChannelsRequestSchema, message)
  );
  const response = fromBinary(
    lnrpc.PendingChannelsResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * ListChannels returns a description of all the open channels that this node
   * is a participant in.
   *
   * @param [ListChannelsRequest]
   * @returns [ListChannelsResponse]
   *
   */
export async function listChannels(
  request: MessageInitShape<typeof lnrpc.ListChannelsRequestSchema>
): Promise<lnrpc.ListChannelsResponse> {
  const message = create(
    lnrpc.ListChannelsRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.listChannels(
    toBinary(lnrpc.ListChannelsRequestSchema, message)
  );
  const response = fromBinary(
    lnrpc.ListChannelsResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * SubscribeChannelEvents creates a uni-directional stream from the server to
   * the client in which any updates relevant to the state of the channels are
   * sent over. Events include new active channels, inactive channels, and closed
   * channels.
   *
   * @param [ChannelEventSubscription]
   * @returns [ChannelEventUpdate]
   *
   */
export function subscribeChannelEvents(
  request: MessageInitShape<typeof lnrpc.ChannelEventSubscriptionSchema>,
  onResponse: (response: lnrpc.ChannelEventUpdate) => void,
  onError: (error: string) => void
): UnsubscribeFromStream {
  const message = create(
    lnrpc.ChannelEventSubscriptionSchema,
    request
  );
  const requestBytes = toBinary(
    lnrpc.ChannelEventSubscriptionSchema,
    message
  );

  const onResponseWrapper: OnResponseCallback = (responseBytes) => {
    onResponse(fromBinary(
      lnrpc.ChannelEventUpdateSchema,
      responseBytes
    ));
  }
  const onErrorWrapper: OnErrorCallback = (error: string) => onError(error);

  return TurboLnd.subscribeChannelEvents(requestBytes, onResponseWrapper, onErrorWrapper);
}


/**
   *
   * ClosedChannels returns a description of all the closed channels that
   * this node was a participant in.
   *
   * @param [ClosedChannelsRequest]
   * @returns [ClosedChannelsResponse]
   *
   */
export async function closedChannels(
  request: MessageInitShape<typeof lnrpc.ClosedChannelsRequestSchema>
): Promise<lnrpc.ClosedChannelsResponse> {
  const message = create(
    lnrpc.ClosedChannelsRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.closedChannels(
    toBinary(lnrpc.ClosedChannelsRequestSchema, message)
  );
  const response = fromBinary(
    lnrpc.ClosedChannelsResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * OpenChannelSync is a synchronous version of the OpenChannel RPC call. This
   * call is meant to be consumed by clients to the REST proxy. As with all
   * other sync calls, all byte slices are intended to be populated as hex
   * encoded strings.
   *
   * @param [OpenChannelRequest]
   * @returns [ChannelPoint]
   *
   */
export async function openChannelSync(
  request: MessageInitShape<typeof lnrpc.OpenChannelRequestSchema>
): Promise<lnrpc.ChannelPoint> {
  const message = create(
    lnrpc.OpenChannelRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.openChannelSync(
    toBinary(lnrpc.OpenChannelRequestSchema, message)
  );
  const response = fromBinary(
    lnrpc.ChannelPointSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * OpenChannel attempts to open a singly funded channel specified in the
   * request to a remote peer. Users are able to specify a target number of
   * blocks that the funding transaction should be confirmed in, or a manual fee
   * rate to us for the funding transaction. If neither are specified, then a
   * lax block confirmation target is used. Each OpenStatusUpdate will return
   * the pending channel ID of the in-progress channel. Depending on the
   * arguments specified in the OpenChannelRequest, this pending channel ID can
   * then be used to manually progress the channel funding flow.
   *
   * @param [OpenChannelRequest]
   * @returns [OpenStatusUpdate]
   *
   */
export function openChannel(
  request: MessageInitShape<typeof lnrpc.OpenChannelRequestSchema>,
  onResponse: (response: lnrpc.OpenStatusUpdate) => void,
  onError: (error: string) => void
): UnsubscribeFromStream {
  const message = create(
    lnrpc.OpenChannelRequestSchema,
    request
  );
  const requestBytes = toBinary(
    lnrpc.OpenChannelRequestSchema,
    message
  );

  const onResponseWrapper: OnResponseCallback = (responseBytes) => {
    onResponse(fromBinary(
      lnrpc.OpenStatusUpdateSchema,
      responseBytes
    ));
  }
  const onErrorWrapper: OnErrorCallback = (error: string) => onError(error);

  return TurboLnd.openChannel(requestBytes, onResponseWrapper, onErrorWrapper);
}


/**
   *
   * BatchOpenChannel attempts to open multiple single-funded channels in a
   * single transaction in an atomic way. This means either all channel open
   * requests succeed at once or all attempts are aborted if any of them fail.
   * This is the safer variant of using PSBTs to manually fund a batch of
   * channels through the OpenChannel RPC.
   *
   * @param [BatchOpenChannelRequest]
   * @returns [BatchOpenChannelResponse]
   *
   */
export async function batchOpenChannel(
  request: MessageInitShape<typeof lnrpc.BatchOpenChannelRequestSchema>
): Promise<lnrpc.BatchOpenChannelResponse> {
  const message = create(
    lnrpc.BatchOpenChannelRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.batchOpenChannel(
    toBinary(lnrpc.BatchOpenChannelRequestSchema, message)
  );
  const response = fromBinary(
    lnrpc.BatchOpenChannelResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * FundingStateStep is an advanced funding related call that allows the caller
   * to either execute some preparatory steps for a funding workflow, or
   * manually progress a funding workflow. The primary way a funding flow is
   * identified is via its pending channel ID. As an example, this method can be
   * used to specify that we're expecting a funding flow for a particular
   * pending channel ID, for which we need to use specific parameters.
   * Alternatively, this can be used to interactively drive PSBT signing for
   * funding for partially complete funding transactions.
   *
   * @param [FundingTransitionMsg]
   * @returns [FundingStateStepResp]
   *
   */
export async function fundingStateStep(
  request: MessageInitShape<typeof lnrpc.FundingTransitionMsgSchema>
): Promise<lnrpc.FundingStateStepResp> {
  const message = create(
    lnrpc.FundingTransitionMsgSchema,
    request
  );
  const responseBytes = await TurboLnd.fundingStateStep(
    toBinary(lnrpc.FundingTransitionMsgSchema, message)
  );
  const response = fromBinary(
    lnrpc.FundingStateStepRespSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * ChannelAcceptor dispatches a bi-directional streaming RPC in which
   * OpenChannel requests are sent to the client and the client responds with
   * a boolean that tells LND whether or not to accept the channel. This allows
   * node operators to specify their own criteria for accepting inbound channels
   * through a single persistent connection.
   *
   * @param [ChannelAcceptResponse]
   * @returns [ChannelAcceptRequest]
   *
   */
export function channelAcceptor(
  onResponse: (response: lnrpc.ChannelAcceptRequest) => void,
  onError: (error: string) => void
) {
  const onResponseWrapper: OnResponseCallback = (responseBytes) => {
    onResponse(fromBinary(
      lnrpc.ChannelAcceptRequestSchema,
      responseBytes
    ));
  }
  const onErrorWrapper: OnErrorCallback = (error: string) => onError(error);

  const writeableStream = TurboLnd.channelAcceptor(onResponseWrapper, onErrorWrapper);

  return {
    send: (response: MessageInitShape<typeof lnrpc.ChannelAcceptResponseSchema>) => {
      const message = create(
        lnrpc.ChannelAcceptResponseSchema,
        response
      );
      const responseBytes = toBinary(
        lnrpc.ChannelAcceptResponseSchema,
        message
      );
      writeableStream.send(responseBytes);
    },
    close: () => {
      writeableStream.stop();
    }
  }
}

/**
   *
   * CloseChannel attempts to close an active channel identified by its channel
   * outpoint (ChannelPoint). The actions of this method can additionally be
   * augmented to attempt a force close after a timeout period in the case of an
   * inactive peer. If a non-force close (cooperative closure) is requested,
   * then the user can specify either a target number of blocks until the
   * closure transaction is confirmed, or a manual fee rate. If neither are
   * specified, then a default lax, block confirmation target is used.
   *
   * @param [CloseChannelRequest]
   * @returns [CloseStatusUpdate]
   *
   */
export function closeChannel(
  request: MessageInitShape<typeof lnrpc.CloseChannelRequestSchema>,
  onResponse: (response: lnrpc.CloseStatusUpdate) => void,
  onError: (error: string) => void
): UnsubscribeFromStream {
  const message = create(
    lnrpc.CloseChannelRequestSchema,
    request
  );
  const requestBytes = toBinary(
    lnrpc.CloseChannelRequestSchema,
    message
  );

  const onResponseWrapper: OnResponseCallback = (responseBytes) => {
    onResponse(fromBinary(
      lnrpc.CloseStatusUpdateSchema,
      responseBytes
    ));
  }
  const onErrorWrapper: OnErrorCallback = (error: string) => onError(error);

  return TurboLnd.closeChannel(requestBytes, onResponseWrapper, onErrorWrapper);
}


/**
   *
   * AbandonChannel removes all channel state from the database except for a
   * close summary. This method can be used to get rid of permanently unusable
   * channels due to bugs fixed in newer versions of lnd. This method can also be
   * used to remove externally funded channels where the funding transaction was
   * never broadcast. Only available for non-externally funded channels in dev
   * build.
   *
   * @param [AbandonChannelRequest]
   * @returns [AbandonChannelResponse]
   *
   */
export async function abandonChannel(
  request: MessageInitShape<typeof lnrpc.AbandonChannelRequestSchema>
): Promise<lnrpc.AbandonChannelResponse> {
  const message = create(
    lnrpc.AbandonChannelRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.abandonChannel(
    toBinary(lnrpc.AbandonChannelRequestSchema, message)
  );
  const response = fromBinary(
    lnrpc.AbandonChannelResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * Deprecated, use SendPaymentV2. SendPayment attempts to route a payment
   * described by the passed PaymentRequest to the final destination. The call
   * returns a stream of payment status updates.
   *
   * @param [SendRequest]
   * @returns [SendResponse]
   *
   */
export function sendPayment(
  onResponse: (response: lnrpc.SendResponse) => void,
  onError: (error: string) => void
) {
  const onResponseWrapper: OnResponseCallback = (responseBytes) => {
    onResponse(fromBinary(
      lnrpc.SendResponseSchema,
      responseBytes
    ));
  }
  const onErrorWrapper: OnErrorCallback = (error: string) => onError(error);

  const writeableStream = TurboLnd.sendPayment(onResponseWrapper, onErrorWrapper);

  return {
    send: (response: MessageInitShape<typeof lnrpc.SendRequestSchema>) => {
      const message = create(
        lnrpc.SendRequestSchema,
        response
      );
      const responseBytes = toBinary(
        lnrpc.SendRequestSchema,
        message
      );
      writeableStream.send(responseBytes);
    },
    close: () => {
      writeableStream.stop();
    }
  }
}

/**
   *
   * SendPaymentSync is the synchronous non-streaming version of SendPayment.
   * This RPC is intended to be consumed by clients of the REST proxy.
   * Additionally, this RPC expects the destination's public key and the payment
   * hash (if any) to be encoded as hex strings.
   *
   * @param [SendRequest]
   * @returns [SendResponse]
   *
   */
export async function sendPaymentSync(
  request: MessageInitShape<typeof lnrpc.SendRequestSchema>
): Promise<lnrpc.SendResponse> {
  const message = create(
    lnrpc.SendRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.sendPaymentSync(
    toBinary(lnrpc.SendRequestSchema, message)
  );
  const response = fromBinary(
    lnrpc.SendResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * Deprecated, use SendToRouteV2. SendToRoute attempts to make a payment via
   * the specified route. This method differs from SendPayment in that it
   * allows users to specify a full route manually. This can be used for
   * things like rebalancing, and atomic swaps. It differs from the newer
   * SendToRouteV2 in that it doesn't return the full HTLC information.
   *
   * @param [SendToRouteRequest]
   * @returns [SendResponse]
   *
   */
export function sendToRoute(
  onResponse: (response: lnrpc.SendResponse) => void,
  onError: (error: string) => void
) {
  const onResponseWrapper: OnResponseCallback = (responseBytes) => {
    onResponse(fromBinary(
      lnrpc.SendResponseSchema,
      responseBytes
    ));
  }
  const onErrorWrapper: OnErrorCallback = (error: string) => onError(error);

  const writeableStream = TurboLnd.sendToRoute(onResponseWrapper, onErrorWrapper);

  return {
    send: (response: MessageInitShape<typeof lnrpc.SendToRouteRequestSchema>) => {
      const message = create(
        lnrpc.SendToRouteRequestSchema,
        response
      );
      const responseBytes = toBinary(
        lnrpc.SendToRouteRequestSchema,
        message
      );
      writeableStream.send(responseBytes);
    },
    close: () => {
      writeableStream.stop();
    }
  }
}

/**
   *
   * SendToRouteSync is a synchronous version of SendToRoute. It Will block
   * until the payment either fails or succeeds.
   *
   * @param [SendToRouteRequest]
   * @returns [SendResponse]
   *
   */
export async function sendToRouteSync(
  request: MessageInitShape<typeof lnrpc.SendToRouteRequestSchema>
): Promise<lnrpc.SendResponse> {
  const message = create(
    lnrpc.SendToRouteRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.sendToRouteSync(
    toBinary(lnrpc.SendToRouteRequestSchema, message)
  );
  const response = fromBinary(
    lnrpc.SendResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * AddInvoice attempts to add a new invoice to the invoice database. Any
   * duplicated invoices are rejected, therefore all invoices *must* have a
   * unique payment preimage.
   *
   * @param [Invoice]
   * @returns [AddInvoiceResponse]
   *
   */
export async function addInvoice(
  request: MessageInitShape<typeof lnrpc.InvoiceSchema>
): Promise<lnrpc.AddInvoiceResponse> {
  const message = create(
    lnrpc.InvoiceSchema,
    request
  );
  const responseBytes = await TurboLnd.addInvoice(
    toBinary(lnrpc.InvoiceSchema, message)
  );
  const response = fromBinary(
    lnrpc.AddInvoiceResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * ListInvoices returns a list of all the invoices currently stored within the
   * database. Any active debug invoices are ignored. It has full support for
   * paginated responses, allowing users to query for specific invoices through
   * their add_index. This can be done by using either the first_index_offset or
   * last_index_offset fields included in the response as the index_offset of the
   * next request. By default, the first 100 invoices created will be returned.
   * Backwards pagination is also supported through the Reversed flag.
   *
   * @param [ListInvoiceRequest]
   * @returns [ListInvoiceResponse]
   *
   */
export async function listInvoices(
  request: MessageInitShape<typeof lnrpc.ListInvoiceRequestSchema>
): Promise<lnrpc.ListInvoiceResponse> {
  const message = create(
    lnrpc.ListInvoiceRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.listInvoices(
    toBinary(lnrpc.ListInvoiceRequestSchema, message)
  );
  const response = fromBinary(
    lnrpc.ListInvoiceResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * LookupInvoice attempts to look up an invoice according to its payment hash.
   * The passed payment hash *must* be exactly 32 bytes, if not, an error is
   * returned.
   *
   * @param [PaymentHash]
   * @returns [Invoice]
   *
   */
export async function lookupInvoice(
  request: MessageInitShape<typeof lnrpc.PaymentHashSchema>
): Promise<lnrpc.Invoice> {
  const message = create(
    lnrpc.PaymentHashSchema,
    request
  );
  const responseBytes = await TurboLnd.lookupInvoice(
    toBinary(lnrpc.PaymentHashSchema, message)
  );
  const response = fromBinary(
    lnrpc.InvoiceSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * SubscribeInvoices returns a uni-directional stream (server -> client) for
   * notifying the client of newly added/settled invoices. The caller can
   * optionally specify the add_index and/or the settle_index. If the add_index
   * is specified, then we'll first start by sending add invoice events for all
   * invoices with an add_index greater than the specified value. If the
   * settle_index is specified, then next, we'll send out all settle events for
   * invoices with a settle_index greater than the specified value. One or both
   * of these fields can be set. If no fields are set, then we'll only send out
   * the latest add/settle events.
   *
   * @param [InvoiceSubscription]
   * @returns [Invoice]
   *
   */
export function subscribeInvoices(
  request: MessageInitShape<typeof lnrpc.InvoiceSubscriptionSchema>,
  onResponse: (response: lnrpc.Invoice) => void,
  onError: (error: string) => void
): UnsubscribeFromStream {
  const message = create(
    lnrpc.InvoiceSubscriptionSchema,
    request
  );
  const requestBytes = toBinary(
    lnrpc.InvoiceSubscriptionSchema,
    message
  );

  const onResponseWrapper: OnResponseCallback = (responseBytes) => {
    onResponse(fromBinary(
      lnrpc.InvoiceSchema,
      responseBytes
    ));
  }
  const onErrorWrapper: OnErrorCallback = (error: string) => onError(error);

  return TurboLnd.subscribeInvoices(requestBytes, onResponseWrapper, onErrorWrapper);
}


/**
   *
   * DecodePayReq takes an encoded payment request string and attempts to decode
   * it, returning a full description of the conditions encoded within the
   * payment request.
   *
   * @param [PayReqString]
   * @returns [PayReq]
   *
   */
export async function decodePayReq(
  request: MessageInitShape<typeof lnrpc.PayReqStringSchema>
): Promise<lnrpc.PayReq> {
  const message = create(
    lnrpc.PayReqStringSchema,
    request
  );
  const responseBytes = await TurboLnd.decodePayReq(
    toBinary(lnrpc.PayReqStringSchema, message)
  );
  const response = fromBinary(
    lnrpc.PayReqSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * ListPayments returns a list of all outgoing payments.
   *
   * @param [ListPaymentsRequest]
   * @returns [ListPaymentsResponse]
   *
   */
export async function listPayments(
  request: MessageInitShape<typeof lnrpc.ListPaymentsRequestSchema>
): Promise<lnrpc.ListPaymentsResponse> {
  const message = create(
    lnrpc.ListPaymentsRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.listPayments(
    toBinary(lnrpc.ListPaymentsRequestSchema, message)
  );
  const response = fromBinary(
    lnrpc.ListPaymentsResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * DeletePayment deletes an outgoing payment from DB. Note that it will not
   * attempt to delete an In-Flight payment, since that would be unsafe.
   *
   * @param [DeletePaymentRequest]
   * @returns [DeletePaymentResponse]
   *
   */
export async function deletePayment(
  request: MessageInitShape<typeof lnrpc.DeletePaymentRequestSchema>
): Promise<lnrpc.DeletePaymentResponse> {
  const message = create(
    lnrpc.DeletePaymentRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.deletePayment(
    toBinary(lnrpc.DeletePaymentRequestSchema, message)
  );
  const response = fromBinary(
    lnrpc.DeletePaymentResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * DeleteAllPayments deletes all outgoing payments from DB. Note that it will
   * not attempt to delete In-Flight payments, since that would be unsafe.
   *
   * @param [DeleteAllPaymentsRequest]
   * @returns [DeleteAllPaymentsResponse]
   *
   */
export async function deleteAllPayments(
  request: MessageInitShape<typeof lnrpc.DeleteAllPaymentsRequestSchema>
): Promise<lnrpc.DeleteAllPaymentsResponse> {
  const message = create(
    lnrpc.DeleteAllPaymentsRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.deleteAllPayments(
    toBinary(lnrpc.DeleteAllPaymentsRequestSchema, message)
  );
  const response = fromBinary(
    lnrpc.DeleteAllPaymentsResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * DescribeGraph returns a description of the latest graph state from the
   * point of view of the node. The graph information is partitioned into two
   * components: all the nodes/vertexes, and all the edges that connect the
   * vertexes themselves. As this is a directed graph, the edges also contain
   * the node directional specific routing policy which includes: the time lock
   * delta, fee information, etc.
   *
   * @param [ChannelGraphRequest]
   * @returns [ChannelGraph]
   *
   */
export async function describeGraph(
  request: MessageInitShape<typeof lnrpc.ChannelGraphRequestSchema>
): Promise<lnrpc.ChannelGraph> {
  const message = create(
    lnrpc.ChannelGraphRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.describeGraph(
    toBinary(lnrpc.ChannelGraphRequestSchema, message)
  );
  const response = fromBinary(
    lnrpc.ChannelGraphSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * GetNodeMetrics returns node metrics calculated from the graph. Currently
   * the only supported metric is betweenness centrality of individual nodes.
   *
   * @param [NodeMetricsRequest]
   * @returns [NodeMetricsResponse]
   *
   */
export async function getNodeMetrics(
  request: MessageInitShape<typeof lnrpc.NodeMetricsRequestSchema>
): Promise<lnrpc.NodeMetricsResponse> {
  const message = create(
    lnrpc.NodeMetricsRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.getNodeMetrics(
    toBinary(lnrpc.NodeMetricsRequestSchema, message)
  );
  const response = fromBinary(
    lnrpc.NodeMetricsResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * GetChanInfo returns the latest authenticated network announcement for the
   * given channel identified by its channel ID: an 8-byte integer which
   * uniquely identifies the location of transaction's funding output within the
   * blockchain.
   *
   * @param [ChanInfoRequest]
   * @returns [ChannelEdge]
   *
   */
export async function getChanInfo(
  request: MessageInitShape<typeof lnrpc.ChanInfoRequestSchema>
): Promise<lnrpc.ChannelEdge> {
  const message = create(
    lnrpc.ChanInfoRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.getChanInfo(
    toBinary(lnrpc.ChanInfoRequestSchema, message)
  );
  const response = fromBinary(
    lnrpc.ChannelEdgeSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * GetNodeInfo returns the latest advertised, aggregated, and authenticated
   * channel information for the specified node identified by its public key.
   *
   * @param [NodeInfoRequest]
   * @returns [NodeInfo]
   *
   */
export async function getNodeInfo(
  request: MessageInitShape<typeof lnrpc.NodeInfoRequestSchema>
): Promise<lnrpc.NodeInfo> {
  const message = create(
    lnrpc.NodeInfoRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.getNodeInfo(
    toBinary(lnrpc.NodeInfoRequestSchema, message)
  );
  const response = fromBinary(
    lnrpc.NodeInfoSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * QueryRoutes attempts to query the daemon's Channel Router for a possible
   * route to a target destination capable of carrying a specific amount of
   * satoshis. The returned route contains the full details required to craft and
   * send an HTLC, also including the necessary information that should be
   * present within the Sphinx packet encapsulated within the HTLC.
   *
   * When using REST, the `dest_custom_records` map type can be set by appending
   * `&dest_custom_records[<record_number>]=<record_data_base64_url_encoded>`
   * to the URL. Unfortunately this map type doesn't appear in the REST API
   * documentation because of a bug in the grpc-gateway library.
   *
   * @param [QueryRoutesRequest]
   * @returns [QueryRoutesResponse]
   *
   */
export async function queryRoutes(
  request: MessageInitShape<typeof lnrpc.QueryRoutesRequestSchema>
): Promise<lnrpc.QueryRoutesResponse> {
  const message = create(
    lnrpc.QueryRoutesRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.queryRoutes(
    toBinary(lnrpc.QueryRoutesRequestSchema, message)
  );
  const response = fromBinary(
    lnrpc.QueryRoutesResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * GetNetworkInfo returns some basic stats about the known channel graph from
   * the point of view of the node.
   *
   * @param [NetworkInfoRequest]
   * @returns [NetworkInfo]
   *
   */
export async function getNetworkInfo(
  request: MessageInitShape<typeof lnrpc.NetworkInfoRequestSchema>
): Promise<lnrpc.NetworkInfo> {
  const message = create(
    lnrpc.NetworkInfoRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.getNetworkInfo(
    toBinary(lnrpc.NetworkInfoRequestSchema, message)
  );
  const response = fromBinary(
    lnrpc.NetworkInfoSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * StopDaemon will send a shutdown request to the interrupt handler, triggering
   * a graceful shutdown of the daemon.
   *
   * @param [StopRequest]
   * @returns [StopResponse]
   *
   */
export async function stopDaemon(
  request: MessageInitShape<typeof lnrpc.StopRequestSchema>
): Promise<lnrpc.StopResponse> {
  const message = create(
    lnrpc.StopRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.stopDaemon(
    toBinary(lnrpc.StopRequestSchema, message)
  );
  const response = fromBinary(
    lnrpc.StopResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * SubscribeChannelGraph launches a streaming RPC that allows the caller to
   * receive notifications upon any changes to the channel graph topology from
   * the point of view of the responding node. Events notified include: new
   * nodes coming online, nodes updating their authenticated attributes, new
   * channels being advertised, updates in the routing policy for a directional
   * channel edge, and when channels are closed on-chain.
   *
   * @param [GraphTopologySubscription]
   * @returns [GraphTopologyUpdate]
   *
   */
export function subscribeChannelGraph(
  request: MessageInitShape<typeof lnrpc.GraphTopologySubscriptionSchema>,
  onResponse: (response: lnrpc.GraphTopologyUpdate) => void,
  onError: (error: string) => void
): UnsubscribeFromStream {
  const message = create(
    lnrpc.GraphTopologySubscriptionSchema,
    request
  );
  const requestBytes = toBinary(
    lnrpc.GraphTopologySubscriptionSchema,
    message
  );

  const onResponseWrapper: OnResponseCallback = (responseBytes) => {
    onResponse(fromBinary(
      lnrpc.GraphTopologyUpdateSchema,
      responseBytes
    ));
  }
  const onErrorWrapper: OnErrorCallback = (error: string) => onError(error);

  return TurboLnd.subscribeChannelGraph(requestBytes, onResponseWrapper, onErrorWrapper);
}


/**
   *
   * DebugLevel allows a caller to programmatically set the logging verbosity of
   * lnd. The logging can be targeted according to a coarse daemon-wide logging
   * level, or in a granular fashion to specify the logging for a target
   * sub-system.
   *
   * @param [DebugLevelRequest]
   * @returns [DebugLevelResponse]
   *
   */
export async function debugLevel(
  request: MessageInitShape<typeof lnrpc.DebugLevelRequestSchema>
): Promise<lnrpc.DebugLevelResponse> {
  const message = create(
    lnrpc.DebugLevelRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.debugLevel(
    toBinary(lnrpc.DebugLevelRequestSchema, message)
  );
  const response = fromBinary(
    lnrpc.DebugLevelResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * FeeReport allows the caller to obtain a report detailing the current fee
   * schedule enforced by the node globally for each channel.
   *
   * @param [FeeReportRequest]
   * @returns [FeeReportResponse]
   *
   */
export async function feeReport(
  request: MessageInitShape<typeof lnrpc.FeeReportRequestSchema>
): Promise<lnrpc.FeeReportResponse> {
  const message = create(
    lnrpc.FeeReportRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.feeReport(
    toBinary(lnrpc.FeeReportRequestSchema, message)
  );
  const response = fromBinary(
    lnrpc.FeeReportResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * UpdateChannelPolicy allows the caller to update the fee schedule and
   * channel policies for all channels globally, or a particular channel.
   *
   * @param [PolicyUpdateRequest]
   * @returns [PolicyUpdateResponse]
   *
   */
export async function updateChannelPolicy(
  request: MessageInitShape<typeof lnrpc.PolicyUpdateRequestSchema>
): Promise<lnrpc.PolicyUpdateResponse> {
  const message = create(
    lnrpc.PolicyUpdateRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.updateChannelPolicy(
    toBinary(lnrpc.PolicyUpdateRequestSchema, message)
  );
  const response = fromBinary(
    lnrpc.PolicyUpdateResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * ForwardingHistory allows the caller to query the htlcswitch for a record of
   * all HTLCs forwarded within the target time range, and integer offset
   * within that time range, for a maximum number of events. If no maximum number
   * of events is specified, up to 100 events will be returned. If no time-range
   * is specified, then events will be returned in the order that they occured.
   *
   * A list of forwarding events are returned. The size of each forwarding event
   * is 40 bytes, and the max message size able to be returned in gRPC is 4 MiB.
   * As a result each message can only contain 50k entries. Each response has
   * the index offset of the last entry. The index offset can be provided to the
   * request to allow the caller to skip a series of records.
   *
   * @param [ForwardingHistoryRequest]
   * @returns [ForwardingHistoryResponse]
   *
   */
export async function forwardingHistory(
  request: MessageInitShape<typeof lnrpc.ForwardingHistoryRequestSchema>
): Promise<lnrpc.ForwardingHistoryResponse> {
  const message = create(
    lnrpc.ForwardingHistoryRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.forwardingHistory(
    toBinary(lnrpc.ForwardingHistoryRequestSchema, message)
  );
  const response = fromBinary(
    lnrpc.ForwardingHistoryResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * ExportChannelBackup attempts to return an encrypted static channel backup
   * for the target channel identified by it channel point. The backup is
   * encrypted with a key generated from the aezeed seed of the user. The
   * returned backup can either be restored using the RestoreChannelBackup
   * method once lnd is running, or via the InitWallet and UnlockWallet methods
   * from the WalletUnlocker service.
   *
   * @param [ExportChannelBackupRequest]
   * @returns [ChannelBackup]
   *
   */
export async function exportChannelBackup(
  request: MessageInitShape<typeof lnrpc.ExportChannelBackupRequestSchema>
): Promise<lnrpc.ChannelBackup> {
  const message = create(
    lnrpc.ExportChannelBackupRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.exportChannelBackup(
    toBinary(lnrpc.ExportChannelBackupRequestSchema, message)
  );
  const response = fromBinary(
    lnrpc.ChannelBackupSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * ExportAllChannelBackups returns static channel backups for all existing
   * channels known to lnd. A set of regular singular static channel backups for
   * each channel are returned. Additionally, a multi-channel backup is returned
   * as well, which contains a single encrypted blob containing the backups of
   * each channel.
   *
   * @param [ChanBackupExportRequest]
   * @returns [ChanBackupSnapshot]
   *
   */
export async function exportAllChannelBackups(
  request: MessageInitShape<typeof lnrpc.ChanBackupExportRequestSchema>
): Promise<lnrpc.ChanBackupSnapshot> {
  const message = create(
    lnrpc.ChanBackupExportRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.exportAllChannelBackups(
    toBinary(lnrpc.ChanBackupExportRequestSchema, message)
  );
  const response = fromBinary(
    lnrpc.ChanBackupSnapshotSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * VerifyChanBackup allows a caller to verify the integrity of a channel backup
   * snapshot. This method will accept either a packed Single or a packed Multi.
   * Specifying both will result in an error.
   *
   * @param [ChanBackupSnapshot]
   * @returns [VerifyChanBackupResponse]
   *
   */
export async function verifyChanBackup(
  request: MessageInitShape<typeof lnrpc.ChanBackupSnapshotSchema>
): Promise<lnrpc.VerifyChanBackupResponse> {
  const message = create(
    lnrpc.ChanBackupSnapshotSchema,
    request
  );
  const responseBytes = await TurboLnd.verifyChanBackup(
    toBinary(lnrpc.ChanBackupSnapshotSchema, message)
  );
  const response = fromBinary(
    lnrpc.VerifyChanBackupResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * RestoreChannelBackups accepts a set of singular channel backups, or a
   * single encrypted multi-chan backup and attempts to recover any funds
   * remaining within the channel. If we are able to unpack the backup, then the
   * new channel will be shown under listchannels, as well as pending channels.
   *
   * @param [RestoreChanBackupRequest]
   * @returns [RestoreBackupResponse]
   *
   */
export async function restoreChannelBackups(
  request: MessageInitShape<typeof lnrpc.RestoreChanBackupRequestSchema>
): Promise<lnrpc.RestoreBackupResponse> {
  const message = create(
    lnrpc.RestoreChanBackupRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.restoreChannelBackups(
    toBinary(lnrpc.RestoreChanBackupRequestSchema, message)
  );
  const response = fromBinary(
    lnrpc.RestoreBackupResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * SubscribeChannelBackups allows a client to sub-subscribe to the most up to
   * date information concerning the state of all channel backups. Each time a
   * new channel is added, we return the new set of channels, along with a
   * multi-chan backup containing the backup info for all channels. Each time a
   * channel is closed, we send a new update, which contains new new chan back
   * ups, but the updated set of encrypted multi-chan backups with the closed
   * channel(s) removed.
   *
   * @param [ChannelBackupSubscription]
   * @returns [ChanBackupSnapshot]
   *
   */
export function subscribeChannelBackups(
  request: MessageInitShape<typeof lnrpc.ChannelBackupSubscriptionSchema>,
  onResponse: (response: lnrpc.ChanBackupSnapshot) => void,
  onError: (error: string) => void
): UnsubscribeFromStream {
  const message = create(
    lnrpc.ChannelBackupSubscriptionSchema,
    request
  );
  const requestBytes = toBinary(
    lnrpc.ChannelBackupSubscriptionSchema,
    message
  );

  const onResponseWrapper: OnResponseCallback = (responseBytes) => {
    onResponse(fromBinary(
      lnrpc.ChanBackupSnapshotSchema,
      responseBytes
    ));
  }
  const onErrorWrapper: OnErrorCallback = (error: string) => onError(error);

  return TurboLnd.subscribeChannelBackups(requestBytes, onResponseWrapper, onErrorWrapper);
}


/**
   *
   * BakeMacaroon allows the creation of a new macaroon with custom read and
   * write permissions. No first-party caveats are added since this can be done
   * offline.
   *
   * @param [BakeMacaroonRequest]
   * @returns [BakeMacaroonResponse]
   *
   */
export async function bakeMacaroon(
  request: MessageInitShape<typeof lnrpc.BakeMacaroonRequestSchema>
): Promise<lnrpc.BakeMacaroonResponse> {
  const message = create(
    lnrpc.BakeMacaroonRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.bakeMacaroon(
    toBinary(lnrpc.BakeMacaroonRequestSchema, message)
  );
  const response = fromBinary(
    lnrpc.BakeMacaroonResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * ListMacaroonIDs returns all root key IDs that are in use.
   *
   * @param [ListMacaroonIDsRequest]
   * @returns [ListMacaroonIDsResponse]
   *
   */
export async function listMacaroonIDs(
  request: MessageInitShape<typeof lnrpc.ListMacaroonIDsRequestSchema>
): Promise<lnrpc.ListMacaroonIDsResponse> {
  const message = create(
    lnrpc.ListMacaroonIDsRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.listMacaroonIDs(
    toBinary(lnrpc.ListMacaroonIDsRequestSchema, message)
  );
  const response = fromBinary(
    lnrpc.ListMacaroonIDsResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * DeleteMacaroonID deletes the specified macaroon ID and invalidates all
   * macaroons derived from that ID.
   *
   * @param [DeleteMacaroonIDRequest]
   * @returns [DeleteMacaroonIDResponse]
   *
   */
export async function deleteMacaroonID(
  request: MessageInitShape<typeof lnrpc.DeleteMacaroonIDRequestSchema>
): Promise<lnrpc.DeleteMacaroonIDResponse> {
  const message = create(
    lnrpc.DeleteMacaroonIDRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.deleteMacaroonID(
    toBinary(lnrpc.DeleteMacaroonIDRequestSchema, message)
  );
  const response = fromBinary(
    lnrpc.DeleteMacaroonIDResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * ListPermissions lists all RPC method URIs and their required macaroon
   * permissions to access them.
   *
   * @param [ListPermissionsRequest]
   * @returns [ListPermissionsResponse]
   *
   */
export async function listPermissions(
  request: MessageInitShape<typeof lnrpc.ListPermissionsRequestSchema>
): Promise<lnrpc.ListPermissionsResponse> {
  const message = create(
    lnrpc.ListPermissionsRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.listPermissions(
    toBinary(lnrpc.ListPermissionsRequestSchema, message)
  );
  const response = fromBinary(
    lnrpc.ListPermissionsResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * CheckMacaroonPermissions checks whether a request follows the constraints
   * imposed on the macaroon and that the macaroon is authorized to follow the
   * provided permissions.
   *
   * @param [CheckMacPermRequest]
   * @returns [CheckMacPermResponse]
   *
   */
export async function checkMacaroonPermissions(
  request: MessageInitShape<typeof lnrpc.CheckMacPermRequestSchema>
): Promise<lnrpc.CheckMacPermResponse> {
  const message = create(
    lnrpc.CheckMacPermRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.checkMacaroonPermissions(
    toBinary(lnrpc.CheckMacPermRequestSchema, message)
  );
  const response = fromBinary(
    lnrpc.CheckMacPermResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * RegisterRPCMiddleware adds a new gRPC middleware to the interceptor chain. A
   * gRPC middleware is software component external to lnd that aims to add
   * additional business logic to lnd by observing/intercepting/validating
   * incoming gRPC client requests and (if needed) replacing/overwriting outgoing
   * messages before they're sent to the client. When registering the middleware
   * must identify itself and indicate what custom macaroon caveats it wants to
   * be responsible for. Only requests that contain a macaroon with that specific
   * custom caveat are then sent to the middleware for inspection. The other
   * option is to register for the read-only mode in which all requests/responses
   * are forwarded for interception to the middleware but the middleware is not
   * allowed to modify any responses. As a security measure, _no_ middleware can
   * modify responses for requests made with _unencumbered_ macaroons!
   *
   * @param [RPCMiddlewareResponse]
   * @returns [RPCMiddlewareRequest]
   *
   */
export function registerRPCMiddleware(
  onResponse: (response: lnrpc.RPCMiddlewareRequest) => void,
  onError: (error: string) => void
) {
  const onResponseWrapper: OnResponseCallback = (responseBytes) => {
    onResponse(fromBinary(
      lnrpc.RPCMiddlewareRequestSchema,
      responseBytes
    ));
  }
  const onErrorWrapper: OnErrorCallback = (error: string) => onError(error);

  const writeableStream = TurboLnd.registerRPCMiddleware(onResponseWrapper, onErrorWrapper);

  return {
    send: (response: MessageInitShape<typeof lnrpc.RPCMiddlewareResponseSchema>) => {
      const message = create(
        lnrpc.RPCMiddlewareResponseSchema,
        response
      );
      const responseBytes = toBinary(
        lnrpc.RPCMiddlewareResponseSchema,
        message
      );
      writeableStream.send(responseBytes);
    },
    close: () => {
      writeableStream.stop();
    }
  }
}

/**
   *
   * SendCustomMessage sends a custom peer message.
   *
   * @param [SendCustomMessageRequest]
   * @returns [SendCustomMessageResponse]
   *
   */
export async function sendCustomMessage(
  request: MessageInitShape<typeof lnrpc.SendCustomMessageRequestSchema>
): Promise<lnrpc.SendCustomMessageResponse> {
  const message = create(
    lnrpc.SendCustomMessageRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.sendCustomMessage(
    toBinary(lnrpc.SendCustomMessageRequestSchema, message)
  );
  const response = fromBinary(
    lnrpc.SendCustomMessageResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * SubscribeCustomMessages subscribes to a stream of incoming custom peer
   * messages.
   *
   * To include messages with type outside of the custom range (>= 32768) lnd
   * needs to be compiled with  the `dev` build tag, and the message type to
   * override should be specified in lnd's experimental protocol configuration.
   *
   * @param [SubscribeCustomMessagesRequest]
   * @returns [CustomMessage]
   *
   */
export function subscribeCustomMessages(
  request: MessageInitShape<typeof lnrpc.SubscribeCustomMessagesRequestSchema>,
  onResponse: (response: lnrpc.CustomMessage) => void,
  onError: (error: string) => void
): UnsubscribeFromStream {
  const message = create(
    lnrpc.SubscribeCustomMessagesRequestSchema,
    request
  );
  const requestBytes = toBinary(
    lnrpc.SubscribeCustomMessagesRequestSchema,
    message
  );

  const onResponseWrapper: OnResponseCallback = (responseBytes) => {
    onResponse(fromBinary(
      lnrpc.CustomMessageSchema,
      responseBytes
    ));
  }
  const onErrorWrapper: OnErrorCallback = (error: string) => onError(error);

  return TurboLnd.subscribeCustomMessages(requestBytes, onResponseWrapper, onErrorWrapper);
}


/**
   *
   * ListAliases returns the set of all aliases that have ever existed with
   * their confirmed SCID (if it exists) and/or the base SCID (in the case of
   * zero conf).
   *
   * @param [ListAliasesRequest]
   * @returns [ListAliasesResponse]
   *
   */
export async function listAliases(
  request: MessageInitShape<typeof lnrpc.ListAliasesRequestSchema>
): Promise<lnrpc.ListAliasesResponse> {
  const message = create(
    lnrpc.ListAliasesRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.listAliases(
    toBinary(lnrpc.ListAliasesRequestSchema, message)
  );
  const response = fromBinary(
    lnrpc.ListAliasesResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * If the replace_response field is set to true, this field must contain the
   * binary serialized gRPC message in the protobuf format.
   *
   * @param [LookupHtlcResolutionRequest]
   * @returns [LookupHtlcResolutionResponse]
   *
   */
export async function lookupHtlcResolution(
  request: MessageInitShape<typeof lnrpc.LookupHtlcResolutionRequestSchema>
): Promise<lnrpc.LookupHtlcResolutionResponse> {
  const message = create(
    lnrpc.LookupHtlcResolutionRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.lookupHtlcResolution(
    toBinary(lnrpc.LookupHtlcResolutionRequestSchema, message)
  );
  const response = fromBinary(
    lnrpc.LookupHtlcResolutionResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * GenSeed is the first method that should be used to instantiate a new lnd
   * instance. This method allows a caller to generate a new aezeed cipher seed
   * given an optional passphrase. If provided, the passphrase will be necessary
   * to decrypt the cipherseed to expose the internal wallet seed.
   *
   * Once the cipherseed is obtained and verified by the user, the InitWallet
   * method should be used to commit the newly generated seed, and create the
   * wallet.
   *
   * @param [GenSeedRequest]
   * @returns [GenSeedResponse]
   *
   */
export async function genSeed(
  request: MessageInitShape<typeof lnrpc.GenSeedRequestSchema>
): Promise<lnrpc.GenSeedResponse> {
  const message = create(
    lnrpc.GenSeedRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.genSeed(
    toBinary(lnrpc.GenSeedRequestSchema, message)
  );
  const response = fromBinary(
    lnrpc.GenSeedResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * InitWallet is used when lnd is starting up for the first time to fully
   * initialize the daemon and its internal wallet. At the very least a wallet
   * password must be provided. This will be used to encrypt sensitive material
   * on disk.
   *
   * In the case of a recovery scenario, the user can also specify their aezeed
   * mnemonic and passphrase. If set, then the daemon will use this prior state
   * to initialize its internal wallet.
   *
   * Alternatively, this can be used along with the GenSeed RPC to obtain a
   * seed, then present it to the user. Once it has been verified by the user,
   * the seed can be fed into this RPC in order to commit the new wallet.
   *
   * @param [InitWalletRequest]
   * @returns [InitWalletResponse]
   *
   */
export async function initWallet(
  request: MessageInitShape<typeof lnrpc.InitWalletRequestSchema>
): Promise<lnrpc.InitWalletResponse> {
  const message = create(
    lnrpc.InitWalletRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.initWallet(
    toBinary(lnrpc.InitWalletRequestSchema, message)
  );
  const response = fromBinary(
    lnrpc.InitWalletResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * UnlockWallet is used at startup of lnd to provide a password to unlock
   * the wallet database.
   *
   * @param [UnlockWalletRequest]
   * @returns [UnlockWalletResponse]
   *
   */
export async function unlockWallet(
  request: MessageInitShape<typeof lnrpc.UnlockWalletRequestSchema>
): Promise<lnrpc.UnlockWalletResponse> {
  const message = create(
    lnrpc.UnlockWalletRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.unlockWallet(
    toBinary(lnrpc.UnlockWalletRequestSchema, message)
  );
  const response = fromBinary(
    lnrpc.UnlockWalletResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * The binary serialized admin macaroon that can be used to access the daemon
   * after rotating the macaroon root key. If both the stateless_init and
   * new_macaroon_root_key parameter were set to true, this is the ONLY copy of
   * the macaroon that was created from the new root key and MUST be stored
   * safely by the caller. Otherwise a copy of this macaroon is also persisted on
   * disk by the daemon, together with other macaroon files.
   *
   * @param [ChangePasswordRequest]
   * @returns [ChangePasswordResponse]
   *
   */
export async function changePassword(
  request: MessageInitShape<typeof lnrpc.ChangePasswordRequestSchema>
): Promise<lnrpc.ChangePasswordResponse> {
  const message = create(
    lnrpc.ChangePasswordRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.changePassword(
    toBinary(lnrpc.ChangePasswordRequestSchema, message)
  );
  const response = fromBinary(
    lnrpc.ChangePasswordResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * Comments in this file will be directly parsed into the API
   * Documentation as descriptions of the associated method, message, or field.
   * These descriptions should go right above the definition of the object, and
   * can be in either block or // comment format.
   *
   * An RPC method can be matched to an lncli command by placing a line in the
   * beginning of the description in exactly the following format:
   * lncli: `methodname`
   *
   * Failure to specify the exact name of the command will cause documentation
   * generation to fail.
   *
   * More information on how exactly the gRPC documentation is generated from
   * this proto file can be found here:
   * https://github.com/lightninglabs/lightning-api
   *
   * @param [SubscribeStateRequest]
   * @returns [SubscribeStateResponse]
   *
   */
export function subscribeState(
  request: MessageInitShape<typeof lnrpc.SubscribeStateRequestSchema>,
  onResponse: (response: lnrpc.SubscribeStateResponse) => void,
  onError: (error: string) => void
): UnsubscribeFromStream {
  const message = create(
    lnrpc.SubscribeStateRequestSchema,
    request
  );
  const requestBytes = toBinary(
    lnrpc.SubscribeStateRequestSchema,
    message
  );

  const onResponseWrapper: OnResponseCallback = (responseBytes) => {
    onResponse(fromBinary(
      lnrpc.SubscribeStateResponseSchema,
      responseBytes
    ));
  }
  const onErrorWrapper: OnErrorCallback = (error: string) => onError(error);

  return TurboLnd.subscribeState(requestBytes, onResponseWrapper, onErrorWrapper);
}


/**
   *
   *
   * @param [GetStateRequest]
   * @returns [GetStateResponse]
   */
export async function getState(
  request: MessageInitShape<typeof lnrpc.GetStateRequestSchema>
): Promise<lnrpc.GetStateResponse> {
  const message = create(
    lnrpc.GetStateRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.getState(
    toBinary(lnrpc.GetStateRequestSchema, message)
  );
  const response = fromBinary(
    lnrpc.GetStateResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * Status returns the status of the light client neutrino instance,
   * along with height and hash of the best block, and a list of connected
   * peers.
   *
   * @param [StatusRequest]
   * @returns [StatusResponse]
   *
   */
export async function autopilotStatus(
  request: MessageInitShape<typeof autopilotrpc.StatusRequestSchema>
): Promise<autopilotrpc.StatusResponse> {
  const message = create(
    autopilotrpc.StatusRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.autopilotStatus(
    toBinary(autopilotrpc.StatusRequestSchema, message)
  );
  const response = fromBinary(
    autopilotrpc.StatusResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * ModifyStatus is used to modify the status of the autopilot agent, like
   * enabling or disabling it.
   *
   * @param [ModifyStatusRequest]
   * @returns [ModifyStatusResponse]
   *
   */
export async function autopilotModifyStatus(
  request: MessageInitShape<typeof autopilotrpc.ModifyStatusRequestSchema>
): Promise<autopilotrpc.ModifyStatusResponse> {
  const message = create(
    autopilotrpc.ModifyStatusRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.autopilotModifyStatus(
    toBinary(autopilotrpc.ModifyStatusRequestSchema, message)
  );
  const response = fromBinary(
    autopilotrpc.ModifyStatusResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * QueryScores queries all available autopilot heuristics, in addition to any
   * active combination of these heruristics, for the scores they would give to
   * the given nodes.
   *
   * @param [QueryScoresRequest]
   * @returns [QueryScoresResponse]
   *
   */
export async function autopilotQueryScores(
  request: MessageInitShape<typeof autopilotrpc.QueryScoresRequestSchema>
): Promise<autopilotrpc.QueryScoresResponse> {
  const message = create(
    autopilotrpc.QueryScoresRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.autopilotQueryScores(
    toBinary(autopilotrpc.QueryScoresRequestSchema, message)
  );
  const response = fromBinary(
    autopilotrpc.QueryScoresResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * A map from hex-encoded public keys to scores. Scores must be in the range
   * [0.0, 1.0].
   *
   * @param [SetScoresRequest]
   * @returns [SetScoresResponse]
   *
   */
export async function autopilotSetScores(
  request: MessageInitShape<typeof autopilotrpc.SetScoresRequestSchema>
): Promise<autopilotrpc.SetScoresResponse> {
  const message = create(
    autopilotrpc.SetScoresRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.autopilotSetScores(
    toBinary(autopilotrpc.SetScoresRequestSchema, message)
  );
  const response = fromBinary(
    autopilotrpc.SetScoresResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * RegisterConfirmationsNtfn is a synchronous response-streaming RPC that
   * registers an intent for a client to be notified once a confirmation request
   * has reached its required number of confirmations on-chain.
   *
   * A confirmation request must have a valid output script. It is also possible
   * to give a transaction ID. If the transaction ID is not set, a notification
   * is sent once the output script confirms. If the transaction ID is also set,
   * a notification is sent once the output script confirms in the given
   * transaction.
   *
   * @param [ConfRequest]
   * @returns [ConfEvent]
   *
   */
export function chainNotifierRegisterConfirmationsNtfn(
  request: MessageInitShape<typeof chainrpc.ConfRequestSchema>,
  onResponse: (response: chainrpc.ConfEvent) => void,
  onError: (error: string) => void
): UnsubscribeFromStream {
  const message = create(
    chainrpc.ConfRequestSchema,
    request
  );
  const requestBytes = toBinary(
    chainrpc.ConfRequestSchema,
    message
  );

  const onResponseWrapper: OnResponseCallback = (responseBytes) => {
    onResponse(fromBinary(
      chainrpc.ConfEventSchema,
      responseBytes
    ));
  }
  const onErrorWrapper: OnErrorCallback = (error: string) => onError(error);

  return TurboLnd.chainNotifierRegisterConfirmationsNtfn(requestBytes, onResponseWrapper, onErrorWrapper);
}


/**
   *
   * RegisterSpendNtfn is a synchronous response-streaming RPC that registers an
   * intent for a client to be notification once a spend request has been spent
   * by a transaction that has confirmed on-chain.
   *
   * A client can specify whether the spend request should be for a particular
   * outpoint  or for an output script by specifying a zero outpoint.
   *
   * @param [SpendRequest]
   * @returns [SpendEvent]
   *
   */
export function chainNotifierRegisterSpendNtfn(
  request: MessageInitShape<typeof chainrpc.SpendRequestSchema>,
  onResponse: (response: chainrpc.SpendEvent) => void,
  onError: (error: string) => void
): UnsubscribeFromStream {
  const message = create(
    chainrpc.SpendRequestSchema,
    request
  );
  const requestBytes = toBinary(
    chainrpc.SpendRequestSchema,
    message
  );

  const onResponseWrapper: OnResponseCallback = (responseBytes) => {
    onResponse(fromBinary(
      chainrpc.SpendEventSchema,
      responseBytes
    ));
  }
  const onErrorWrapper: OnErrorCallback = (error: string) => onError(error);

  return TurboLnd.chainNotifierRegisterSpendNtfn(requestBytes, onResponseWrapper, onErrorWrapper);
}


/**
   *
   * An event sent when the spending transaction of the request was
   * reorged out of the chain.
   *
   * @param [BlockEpoch]
   * @returns [BlockEpoch]
   *
   */
export function chainNotifierRegisterBlockEpochNtfn(
  request: MessageInitShape<typeof chainrpc.BlockEpochSchema>,
  onResponse: (response: chainrpc.BlockEpoch) => void,
  onError: (error: string) => void
): UnsubscribeFromStream {
  const message = create(
    chainrpc.BlockEpochSchema,
    request
  );
  const requestBytes = toBinary(
    chainrpc.BlockEpochSchema,
    message
  );

  const onResponseWrapper: OnResponseCallback = (responseBytes) => {
    onResponse(fromBinary(
      chainrpc.BlockEpochSchema,
      responseBytes
    ));
  }
  const onErrorWrapper: OnErrorCallback = (error: string) => onError(error);

  return TurboLnd.chainNotifierRegisterBlockEpochNtfn(requestBytes, onResponseWrapper, onErrorWrapper);
}


/**
   *
   * SubscribeSingleInvoice returns a uni-directional stream (server -> client)
   * to notify the client of state transitions of the specified invoice.
   * Initially the current invoice state is always sent out.
   *
   * @param [SubscribeSingleInvoiceRequest]
   * @returns [Invoice]
   *
   */
export function invoicesSubscribeSingleInvoice(
  request: MessageInitShape<typeof invoicesrpc.SubscribeSingleInvoiceRequestSchema>,
  onResponse: (response: lnrpc.Invoice) => void,
  onError: (error: string) => void
): UnsubscribeFromStream {
  const message = create(
    invoicesrpc.SubscribeSingleInvoiceRequestSchema,
    request
  );
  const requestBytes = toBinary(
    invoicesrpc.SubscribeSingleInvoiceRequestSchema,
    message
  );

  const onResponseWrapper: OnResponseCallback = (responseBytes) => {
    onResponse(fromBinary(
      lnrpc.InvoiceSchema,
      responseBytes
    ));
  }
  const onErrorWrapper: OnErrorCallback = (error: string) => onError(error);

  return TurboLnd.invoicesSubscribeSingleInvoice(requestBytes, onResponseWrapper, onErrorWrapper);
}


/**
   *
   * CancelInvoice cancels a currently open invoice. If the invoice is already
   * canceled, this call will succeed. If the invoice is already settled, it will
   * fail.
   *
   * @param [CancelInvoiceMsg]
   * @returns [CancelInvoiceResp]
   *
   */
export async function invoicesCancelInvoice(
  request: MessageInitShape<typeof invoicesrpc.CancelInvoiceMsgSchema>
): Promise<invoicesrpc.CancelInvoiceResp> {
  const message = create(
    invoicesrpc.CancelInvoiceMsgSchema,
    request
  );
  const responseBytes = await TurboLnd.invoicesCancelInvoice(
    toBinary(invoicesrpc.CancelInvoiceMsgSchema, message)
  );
  const response = fromBinary(
    invoicesrpc.CancelInvoiceRespSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * AddHoldInvoice creates a hold invoice. It ties the invoice to the hash
   * supplied in the request.
   *
   * @param [AddHoldInvoiceRequest]
   * @returns [AddHoldInvoiceResp]
   *
   */
export async function invoicesAddHoldInvoice(
  request: MessageInitShape<typeof invoicesrpc.AddHoldInvoiceRequestSchema>
): Promise<invoicesrpc.AddHoldInvoiceResp> {
  const message = create(
    invoicesrpc.AddHoldInvoiceRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.invoicesAddHoldInvoice(
    toBinary(invoicesrpc.AddHoldInvoiceRequestSchema, message)
  );
  const response = fromBinary(
    invoicesrpc.AddHoldInvoiceRespSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * SettleInvoice settles an accepted invoice. If the invoice is already
   * settled, this call will succeed.
   *
   * @param [SettleInvoiceMsg]
   * @returns [SettleInvoiceResp]
   *
   */
export async function invoicesSettleInvoice(
  request: MessageInitShape<typeof invoicesrpc.SettleInvoiceMsgSchema>
): Promise<invoicesrpc.SettleInvoiceResp> {
  const message = create(
    invoicesrpc.SettleInvoiceMsgSchema,
    request
  );
  const responseBytes = await TurboLnd.invoicesSettleInvoice(
    toBinary(invoicesrpc.SettleInvoiceMsgSchema, message)
  );
  const response = fromBinary(
    invoicesrpc.SettleInvoiceRespSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * LookupInvoiceV2 attempts to look up at invoice. An invoice can be referenced
   * using either its payment hash, payment address, or set ID.
   *
   * @param [LookupInvoiceMsg]
   * @returns [Invoice]
   *
   */
export async function invoicesLookupInvoiceV2(
  request: MessageInitShape<typeof invoicesrpc.LookupInvoiceMsgSchema>
): Promise<lnrpc.Invoice> {
  const message = create(
    invoicesrpc.LookupInvoiceMsgSchema,
    request
  );
  const responseBytes = await TurboLnd.invoicesLookupInvoiceV2(
    toBinary(invoicesrpc.LookupInvoiceMsgSchema, message)
  );
  const response = fromBinary(
    lnrpc.InvoiceSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * Indicates that when a look up is done using a payment_addr, then no HTLCs
   * related to the payment_addr should be returned. This is useful when one
   * wants to be able to obtain the set of associated setIDs with a given
   * invoice, then look up the sub-invoices "projected" by that set ID.
   *
   * @param [HtlcModifyResponse]
   * @returns [HtlcModifyRequest]
   *
   */
export function invoicesHtlcModifier(
  onResponse: (response: invoicesrpc.HtlcModifyRequest) => void,
  onError: (error: string) => void
) {
  const onResponseWrapper: OnResponseCallback = (responseBytes) => {
    onResponse(fromBinary(
      invoicesrpc.HtlcModifyRequestSchema,
      responseBytes
    ));
  }
  const onErrorWrapper: OnErrorCallback = (error: string) => onError(error);

  const writeableStream = TurboLnd.invoicesHtlcModifier(onResponseWrapper, onErrorWrapper);

  return {
    send: (response: MessageInitShape<typeof invoicesrpc.HtlcModifyResponseSchema>) => {
      const message = create(
        invoicesrpc.HtlcModifyResponseSchema,
        response
      );
      const responseBytes = toBinary(
        invoicesrpc.HtlcModifyResponseSchema,
        message
      );
      writeableStream.send(responseBytes);
    },
    close: () => {
      writeableStream.stop();
    }
  }
}

/**
   *
   * Status returns the status of the light client neutrino instance,
   * along with height and hash of the best block, and a list of connected
   * peers.
   *
   * @param [StatusRequest]
   * @returns [StatusResponse]
   *
   */
export async function neutrinoKitStatus(
  request: MessageInitShape<typeof neutrinorpc.StatusRequestSchema>
): Promise<neutrinorpc.StatusResponse> {
  const message = create(
    neutrinorpc.StatusRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.neutrinoKitStatus(
    toBinary(neutrinorpc.StatusRequestSchema, message)
  );
  const response = fromBinary(
    neutrinorpc.StatusResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * AddPeer adds a new peer that has already been connected to the server.
   *
   * @param [AddPeerRequest]
   * @returns [AddPeerResponse]
   *
   */
export async function neutrinoKitAddPeer(
  request: MessageInitShape<typeof neutrinorpc.AddPeerRequestSchema>
): Promise<neutrinorpc.AddPeerResponse> {
  const message = create(
    neutrinorpc.AddPeerRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.neutrinoKitAddPeer(
    toBinary(neutrinorpc.AddPeerRequestSchema, message)
  );
  const response = fromBinary(
    neutrinorpc.AddPeerResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * DisconnectPeer disconnects a peer by target address. Both outbound and
   * inbound nodes will be searched for the target node. An error message will
   * be returned if the peer was not found.
   *
   * @param [DisconnectPeerRequest]
   * @returns [DisconnectPeerResponse]
   *
   */
export async function neutrinoKitDisconnectPeer(
  request: MessageInitShape<typeof neutrinorpc.DisconnectPeerRequestSchema>
): Promise<neutrinorpc.DisconnectPeerResponse> {
  const message = create(
    neutrinorpc.DisconnectPeerRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.neutrinoKitDisconnectPeer(
    toBinary(neutrinorpc.DisconnectPeerRequestSchema, message)
  );
  const response = fromBinary(
    neutrinorpc.DisconnectPeerResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * IsBanned returns true if the peer is banned, otherwise false.
   *
   * @param [IsBannedRequest]
   * @returns [IsBannedResponse]
   *
   */
export async function neutrinoKitIsBanned(
  request: MessageInitShape<typeof neutrinorpc.IsBannedRequestSchema>
): Promise<neutrinorpc.IsBannedResponse> {
  const message = create(
    neutrinorpc.IsBannedRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.neutrinoKitIsBanned(
    toBinary(neutrinorpc.IsBannedRequestSchema, message)
  );
  const response = fromBinary(
    neutrinorpc.IsBannedResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * GetBlockHeader returns a block header with a particular block hash.
   *
   * @param [GetBlockHeaderRequest]
   * @returns [GetBlockHeaderResponse]
   *
   */
export async function neutrinoKitGetBlockHeader(
  request: MessageInitShape<typeof neutrinorpc.GetBlockHeaderRequestSchema>
): Promise<neutrinorpc.GetBlockHeaderResponse> {
  const message = create(
    neutrinorpc.GetBlockHeaderRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.neutrinoKitGetBlockHeader(
    toBinary(neutrinorpc.GetBlockHeaderRequestSchema, message)
  );
  const response = fromBinary(
    neutrinorpc.GetBlockHeaderResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * GetBlock returns a block with a particular block hash.
   *
   * @param [GetBlockRequest]
   * @returns [GetBlockResponse]
   *
   */
export async function neutrinoKitGetBlock(
  request: MessageInitShape<typeof neutrinorpc.GetBlockRequestSchema>
): Promise<neutrinorpc.GetBlockResponse> {
  const message = create(
    neutrinorpc.GetBlockRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.neutrinoKitGetBlock(
    toBinary(neutrinorpc.GetBlockRequestSchema, message)
  );
  const response = fromBinary(
    neutrinorpc.GetBlockResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * GetCFilter returns a compact filter from a block.
   *
   * @param [GetCFilterRequest]
   * @returns [GetCFilterResponse]
   *
   */
export async function neutrinoKitGetCFilter(
  request: MessageInitShape<typeof neutrinorpc.GetCFilterRequestSchema>
): Promise<neutrinorpc.GetCFilterResponse> {
  const message = create(
    neutrinorpc.GetCFilterRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.neutrinoKitGetCFilter(
    toBinary(neutrinorpc.GetCFilterRequestSchema, message)
  );
  const response = fromBinary(
    neutrinorpc.GetCFilterResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * Deprecated, use chainrpc.GetBlockHash instead.
   * GetBlockHash returns the header hash of a block at a given height.
   *
   * @param [GetBlockHashRequest]
   * @returns [GetBlockHashResponse]
   *
   */
export async function neutrinoKitGetBlockHash(
  request: MessageInitShape<typeof neutrinorpc.GetBlockHashRequestSchema>
): Promise<neutrinorpc.GetBlockHashResponse> {
  const message = create(
    neutrinorpc.GetBlockHashRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.neutrinoKitGetBlockHash(
    toBinary(neutrinorpc.GetBlockHashRequestSchema, message)
  );
  const response = fromBinary(
    neutrinorpc.GetBlockHashResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * SET_INVOICE_AMP identifies the features that should be advertised on
   * AMP invoices generated by the daemon.
   *
   * @param [NodeAnnouncementUpdateRequest]
   * @returns [NodeAnnouncementUpdateResponse]
   *
   */
export async function peersUpdateNodeAnnouncement(
  request: MessageInitShape<typeof peersrpc.NodeAnnouncementUpdateRequestSchema>
): Promise<peersrpc.NodeAnnouncementUpdateResponse> {
  const message = create(
    peersrpc.NodeAnnouncementUpdateRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.peersUpdateNodeAnnouncement(
    toBinary(peersrpc.NodeAnnouncementUpdateRequestSchema, message)
  );
  const response = fromBinary(
    peersrpc.NodeAnnouncementUpdateResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * SendPaymentV2 attempts to route a payment described by the passed
   * PaymentRequest to the final destination. The call returns a stream of
   * payment updates. When using this RPC, make sure to set a fee limit, as the
   * default routing fee limit is 0 sats. Without a non-zero fee limit only
   * routes without fees will be attempted which often fails with
   * FAILURE_REASON_NO_ROUTE.
   *
   * @param [SendPaymentRequest]
   * @returns [Payment]
   *
   */
export function routerSendPaymentV2(
  request: MessageInitShape<typeof routerrpc.SendPaymentRequestSchema>,
  onResponse: (response: lnrpc.Payment) => void,
  onError: (error: string) => void
): UnsubscribeFromStream {
  const message = create(
    routerrpc.SendPaymentRequestSchema,
    request
  );
  const requestBytes = toBinary(
    routerrpc.SendPaymentRequestSchema,
    message
  );

  const onResponseWrapper: OnResponseCallback = (responseBytes) => {
    onResponse(fromBinary(
      lnrpc.PaymentSchema,
      responseBytes
    ));
  }
  const onErrorWrapper: OnErrorCallback = (error: string) => onError(error);

  return TurboLnd.routerSendPaymentV2(requestBytes, onResponseWrapper, onErrorWrapper);
}


/**
   *
   * TrackPaymentV2 returns an update stream for the payment identified by the
   * payment hash.
   *
   * @param [TrackPaymentRequest]
   * @returns [Payment]
   *
   */
export function routerTrackPaymentV2(
  request: MessageInitShape<typeof routerrpc.TrackPaymentRequestSchema>,
  onResponse: (response: lnrpc.Payment) => void,
  onError: (error: string) => void
): UnsubscribeFromStream {
  const message = create(
    routerrpc.TrackPaymentRequestSchema,
    request
  );
  const requestBytes = toBinary(
    routerrpc.TrackPaymentRequestSchema,
    message
  );

  const onResponseWrapper: OnResponseCallback = (responseBytes) => {
    onResponse(fromBinary(
      lnrpc.PaymentSchema,
      responseBytes
    ));
  }
  const onErrorWrapper: OnErrorCallback = (error: string) => onError(error);

  return TurboLnd.routerTrackPaymentV2(requestBytes, onResponseWrapper, onErrorWrapper);
}


/**
   *
   * TrackPayments returns an update stream for every payment that is not in a
   * terminal state. Note that if payments are in-flight while starting a new
   * subscription, the start of the payment stream could produce out-of-order
   * and/or duplicate events. In order to get updates for every in-flight
   * payment attempt make sure to subscribe to this method before initiating any
   * payments.
   *
   * @param [TrackPaymentsRequest]
   * @returns [Payment]
   *
   */
export function routerTrackPayments(
  request: MessageInitShape<typeof routerrpc.TrackPaymentsRequestSchema>,
  onResponse: (response: lnrpc.Payment) => void,
  onError: (error: string) => void
): UnsubscribeFromStream {
  const message = create(
    routerrpc.TrackPaymentsRequestSchema,
    request
  );
  const requestBytes = toBinary(
    routerrpc.TrackPaymentsRequestSchema,
    message
  );

  const onResponseWrapper: OnResponseCallback = (responseBytes) => {
    onResponse(fromBinary(
      lnrpc.PaymentSchema,
      responseBytes
    ));
  }
  const onErrorWrapper: OnErrorCallback = (error: string) => onError(error);

  return TurboLnd.routerTrackPayments(requestBytes, onResponseWrapper, onErrorWrapper);
}


/**
   *
   * EstimateRouteFee allows callers to obtain a lower bound w.r.t how much it
   * may cost to send an HTLC to the target end destination.
   *
   * @param [RouteFeeRequest]
   * @returns [RouteFeeResponse]
   *
   */
export async function routerEstimateRouteFee(
  request: MessageInitShape<typeof routerrpc.RouteFeeRequestSchema>
): Promise<routerrpc.RouteFeeResponse> {
  const message = create(
    routerrpc.RouteFeeRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.routerEstimateRouteFee(
    toBinary(routerrpc.RouteFeeRequestSchema, message)
  );
  const response = fromBinary(
    routerrpc.RouteFeeResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * Deprecated, use SendToRouteV2. SendToRoute attempts to make a payment via
   * the specified route. This method differs from SendPayment in that it
   * allows users to specify a full route manually. This can be used for
   * things like rebalancing, and atomic swaps. It differs from the newer
   * SendToRouteV2 in that it doesn't return the full HTLC information.
   *
   * @param [SendToRouteRequest]
   * @returns [SendToRouteResponse]
   *
   */
export async function routerSendToRoute(
  request: MessageInitShape<typeof routerrpc.SendToRouteRequestSchema>
): Promise<routerrpc.SendToRouteResponse> {
  const message = create(
    routerrpc.SendToRouteRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.routerSendToRoute(
    toBinary(routerrpc.SendToRouteRequestSchema, message)
  );
  const response = fromBinary(
    routerrpc.SendToRouteResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * SendToRouteV2 attempts to make a payment via the specified route. This
   * method differs from SendPayment in that it allows users to specify a full
   * route manually. This can be used for things like rebalancing, and atomic
   * swaps.
   *
   * @param [SendToRouteRequest]
   * @returns [HTLCAttempt]
   *
   */
export async function routerSendToRouteV2(
  request: MessageInitShape<typeof routerrpc.SendToRouteRequestSchema>
): Promise<lnrpc.HTLCAttempt> {
  const message = create(
    routerrpc.SendToRouteRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.routerSendToRouteV2(
    toBinary(routerrpc.SendToRouteRequestSchema, message)
  );
  const response = fromBinary(
    lnrpc.HTLCAttemptSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * ResetMissionControl clears all mission control state and starts with a clean
   * slate.
   *
   * @param [ResetMissionControlRequest]
   * @returns [ResetMissionControlResponse]
   *
   */
export async function routerResetMissionControl(
  request: MessageInitShape<typeof routerrpc.ResetMissionControlRequestSchema>
): Promise<routerrpc.ResetMissionControlResponse> {
  const message = create(
    routerrpc.ResetMissionControlRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.routerResetMissionControl(
    toBinary(routerrpc.ResetMissionControlRequestSchema, message)
  );
  const response = fromBinary(
    routerrpc.ResetMissionControlResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * QueryMissionControl exposes the internal mission control state to callers.
   * It is a development feature.
   *
   * @param [QueryMissionControlRequest]
   * @returns [QueryMissionControlResponse]
   *
   */
export async function routerQueryMissionControl(
  request: MessageInitShape<typeof routerrpc.QueryMissionControlRequestSchema>
): Promise<routerrpc.QueryMissionControlResponse> {
  const message = create(
    routerrpc.QueryMissionControlRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.routerQueryMissionControl(
    toBinary(routerrpc.QueryMissionControlRequestSchema, message)
  );
  const response = fromBinary(
    routerrpc.QueryMissionControlResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * XImportMissionControl is an experimental API that imports the state provided
   * to the internal mission control's state, using all results which are more
   * recent than our existing values. These values will only be imported
   * in-memory, and will not be persisted across restarts.
   *
   * @param [XImportMissionControlRequest]
   * @returns [XImportMissionControlResponse]
   *
   */
export async function routerXImportMissionControl(
  request: MessageInitShape<typeof routerrpc.XImportMissionControlRequestSchema>
): Promise<routerrpc.XImportMissionControlResponse> {
  const message = create(
    routerrpc.XImportMissionControlRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.routerXImportMissionControl(
    toBinary(routerrpc.XImportMissionControlRequestSchema, message)
  );
  const response = fromBinary(
    routerrpc.XImportMissionControlResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * GetMissionControlConfig returns mission control's current config.
   *
   * @param [GetMissionControlConfigRequest]
   * @returns [GetMissionControlConfigResponse]
   *
   */
export async function routerGetMissionControlConfig(
  request: MessageInitShape<typeof routerrpc.GetMissionControlConfigRequestSchema>
): Promise<routerrpc.GetMissionControlConfigResponse> {
  const message = create(
    routerrpc.GetMissionControlConfigRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.routerGetMissionControlConfig(
    toBinary(routerrpc.GetMissionControlConfigRequestSchema, message)
  );
  const response = fromBinary(
    routerrpc.GetMissionControlConfigResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * SetMissionControlConfig will set mission control's config, if the config
   * provided is valid.
   *
   * @param [SetMissionControlConfigRequest]
   * @returns [SetMissionControlConfigResponse]
   *
   */
export async function routerSetMissionControlConfig(
  request: MessageInitShape<typeof routerrpc.SetMissionControlConfigRequestSchema>
): Promise<routerrpc.SetMissionControlConfigResponse> {
  const message = create(
    routerrpc.SetMissionControlConfigRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.routerSetMissionControlConfig(
    toBinary(routerrpc.SetMissionControlConfigRequestSchema, message)
  );
  const response = fromBinary(
    routerrpc.SetMissionControlConfigResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * Deprecated. QueryProbability returns the current success probability
   * estimate for a given node pair and amount. The call returns a zero success
   * probability if no channel is available or if the amount violates min/max
   * HTLC constraints.
   *
   * @param [QueryProbabilityRequest]
   * @returns [QueryProbabilityResponse]
   *
   */
export async function routerQueryProbability(
  request: MessageInitShape<typeof routerrpc.QueryProbabilityRequestSchema>
): Promise<routerrpc.QueryProbabilityResponse> {
  const message = create(
    routerrpc.QueryProbabilityRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.routerQueryProbability(
    toBinary(routerrpc.QueryProbabilityRequestSchema, message)
  );
  const response = fromBinary(
    routerrpc.QueryProbabilityResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * BuildRoute builds a fully specified route based on a list of hop public
   * keys. It retrieves the relevant channel policies from the graph in order to
   * calculate the correct fees and time locks.
   * Note that LND will use its default final_cltv_delta if no value is supplied.
   * Make sure to add the correct final_cltv_delta depending on the invoice
   * restriction. Moreover the caller has to make sure to provide the
   * payment_addr if the route is paying an invoice which signaled it.
   *
   * @param [BuildRouteRequest]
   * @returns [BuildRouteResponse]
   *
   */
export async function routerBuildRoute(
  request: MessageInitShape<typeof routerrpc.BuildRouteRequestSchema>
): Promise<routerrpc.BuildRouteResponse> {
  const message = create(
    routerrpc.BuildRouteRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.routerBuildRoute(
    toBinary(routerrpc.BuildRouteRequestSchema, message)
  );
  const response = fromBinary(
    routerrpc.BuildRouteResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * SubscribeHtlcEvents creates a uni-directional stream from the server to
   * the client which delivers a stream of htlc events.
   *
   * @param [SubscribeHtlcEventsRequest]
   * @returns [HtlcEvent]
   *
   */
export function routerSubscribeHtlcEvents(
  request: MessageInitShape<typeof routerrpc.SubscribeHtlcEventsRequestSchema>,
  onResponse: (response: routerrpc.HtlcEvent) => void,
  onError: (error: string) => void
): UnsubscribeFromStream {
  const message = create(
    routerrpc.SubscribeHtlcEventsRequestSchema,
    request
  );
  const requestBytes = toBinary(
    routerrpc.SubscribeHtlcEventsRequestSchema,
    message
  );

  const onResponseWrapper: OnResponseCallback = (responseBytes) => {
    onResponse(fromBinary(
      routerrpc.HtlcEventSchema,
      responseBytes
    ));
  }
  const onErrorWrapper: OnErrorCallback = (error: string) => onError(error);

  return TurboLnd.routerSubscribeHtlcEvents(requestBytes, onResponseWrapper, onErrorWrapper);
}


/**
   *
   * Deprecated, use SendPaymentV2. SendPayment attempts to route a payment
   * described by the passed PaymentRequest to the final destination. The call
   * returns a stream of payment status updates.
   *
   * @param [SendPaymentRequest]
   * @returns [PaymentStatus]
   *
   */
export function routerSendPayment(
  request: MessageInitShape<typeof routerrpc.SendPaymentRequestSchema>,
  onResponse: (response: routerrpc.PaymentStatus) => void,
  onError: (error: string) => void
): UnsubscribeFromStream {
  const message = create(
    routerrpc.SendPaymentRequestSchema,
    request
  );
  const requestBytes = toBinary(
    routerrpc.SendPaymentRequestSchema,
    message
  );

  const onResponseWrapper: OnResponseCallback = (responseBytes) => {
    onResponse(fromBinary(
      routerrpc.PaymentStatusSchema,
      responseBytes
    ));
  }
  const onErrorWrapper: OnErrorCallback = (error: string) => onError(error);

  return TurboLnd.routerSendPayment(requestBytes, onResponseWrapper, onErrorWrapper);
}


/**
   *
   * Deprecated, use TrackPaymentV2. TrackPayment returns an update stream for
   * the payment identified by the payment hash.
   *
   * @param [TrackPaymentRequest]
   * @returns [PaymentStatus]
   *
   */
export function routerTrackPayment(
  request: MessageInitShape<typeof routerrpc.TrackPaymentRequestSchema>,
  onResponse: (response: routerrpc.PaymentStatus) => void,
  onError: (error: string) => void
): UnsubscribeFromStream {
  const message = create(
    routerrpc.TrackPaymentRequestSchema,
    request
  );
  const requestBytes = toBinary(
    routerrpc.TrackPaymentRequestSchema,
    message
  );

  const onResponseWrapper: OnResponseCallback = (responseBytes) => {
    onResponse(fromBinary(
      routerrpc.PaymentStatusSchema,
      responseBytes
    ));
  }
  const onErrorWrapper: OnErrorCallback = (error: string) => onError(error);

  return TurboLnd.routerTrackPayment(requestBytes, onResponseWrapper, onErrorWrapper);
}


/**
   *
   * HtlcInterceptor dispatches a bi-directional streaming RPC in which
   * Forwarded HTLC requests are sent to the client and the client responds with
   * a boolean that tells LND if this htlc should be intercepted.
   * In case of interception, the htlc can be either settled, cancelled or
   * resumed later by using the ResolveHoldForward endpoint.
   *
   * @param [ForwardHtlcInterceptResponse]
   * @returns [ForwardHtlcInterceptRequest]
   *
   */
export function routerHtlcInterceptor(
  onResponse: (response: routerrpc.ForwardHtlcInterceptRequest) => void,
  onError: (error: string) => void
) {
  const onResponseWrapper: OnResponseCallback = (responseBytes) => {
    onResponse(fromBinary(
      routerrpc.ForwardHtlcInterceptRequestSchema,
      responseBytes
    ));
  }
  const onErrorWrapper: OnErrorCallback = (error: string) => onError(error);

  const writeableStream = TurboLnd.routerHtlcInterceptor(onResponseWrapper, onErrorWrapper);

  return {
    send: (response: MessageInitShape<typeof routerrpc.ForwardHtlcInterceptResponseSchema>) => {
      const message = create(
        routerrpc.ForwardHtlcInterceptResponseSchema,
        response
      );
      const responseBytes = toBinary(
        routerrpc.ForwardHtlcInterceptResponseSchema,
        message
      );
      writeableStream.send(responseBytes);
    },
    close: () => {
      writeableStream.stop();
    }
  }
}

/**
   *
   * UpdateChanStatus attempts to manually set the state of a channel
   * (enabled, disabled, or auto). A manual "disable" request will cause the
   * channel to stay disabled until a subsequent manual request of either
   * "enable" or "auto".
   *
   * @param [UpdateChanStatusRequest]
   * @returns [UpdateChanStatusResponse]
   *
   */
export async function routerUpdateChanStatus(
  request: MessageInitShape<typeof routerrpc.UpdateChanStatusRequestSchema>
): Promise<routerrpc.UpdateChanStatusResponse> {
  const message = create(
    routerrpc.UpdateChanStatusRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.routerUpdateChanStatus(
    toBinary(routerrpc.UpdateChanStatusRequestSchema, message)
  );
  const response = fromBinary(
    routerrpc.UpdateChanStatusResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * XAddLocalChanAliases is an experimental API that creates a set of new
   * channel SCID alias mappings. The final total set of aliases in the manager
   * after the add operation is returned. This is only a locally stored alias,
   * and will not be communicated to the channel peer via any message. Therefore,
   * routing over such an alias will only work if the peer also calls this same
   * RPC on their end. If an alias already exists, an error is returned
   *
   * @param [AddAliasesRequest]
   * @returns [AddAliasesResponse]
   *
   */
export async function routerXAddLocalChanAliases(
  request: MessageInitShape<typeof routerrpc.AddAliasesRequestSchema>
): Promise<routerrpc.AddAliasesResponse> {
  const message = create(
    routerrpc.AddAliasesRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.routerXAddLocalChanAliases(
    toBinary(routerrpc.AddAliasesRequestSchema, message)
  );
  const response = fromBinary(
    routerrpc.AddAliasesResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * The key of this forwarded htlc. It defines the incoming channel id and
   * the index in this channel.
   *
   * @param [DeleteAliasesRequest]
   * @returns [DeleteAliasesResponse]
   *
   */
export async function routerXDeleteLocalChanAliases(
  request: MessageInitShape<typeof routerrpc.DeleteAliasesRequestSchema>
): Promise<routerrpc.DeleteAliasesResponse> {
  const message = create(
    routerrpc.DeleteAliasesRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.routerXDeleteLocalChanAliases(
    toBinary(routerrpc.DeleteAliasesRequestSchema, message)
  );
  const response = fromBinary(
    routerrpc.DeleteAliasesResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * SignOutputRaw is a method that can be used to generated a signature for a
   * set of inputs/outputs to a transaction. Each request specifies details
   * concerning how the outputs should be signed, which keys they should be
   * signed with, and also any optional tweaks. The return value is a fixed
   * 64-byte signature (the same format as we use on the wire in Lightning).
   *
   * If we are  unable to sign using the specified keys, then an error will be
   * returned.
   *
   * @param [SignReq]
   * @returns [SignResp]
   *
   */
export async function signerSignOutputRaw(
  request: MessageInitShape<typeof signrpc.SignReqSchema>
): Promise<signrpc.SignResp> {
  const message = create(
    signrpc.SignReqSchema,
    request
  );
  const responseBytes = await TurboLnd.signerSignOutputRaw(
    toBinary(signrpc.SignReqSchema, message)
  );
  const response = fromBinary(
    signrpc.SignRespSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * ComputeInputScript generates a complete InputIndex for the passed
   * transaction with the signature as defined within the passed SignDescriptor.
   * This method should be capable of generating the proper input script for both
   * regular p2wkh/p2tr outputs and p2wkh outputs nested within a regular p2sh
   * output.
   *
   * Note that when using this method to sign inputs belonging to the wallet,
   * the only items of the SignDescriptor that need to be populated are pkScript
   * in the TxOut field, the value in that same field, and finally the input
   * index.
   *
   * @param [SignReq]
   * @returns [InputScriptResp]
   *
   */
export async function signerComputeInputScript(
  request: MessageInitShape<typeof signrpc.SignReqSchema>
): Promise<signrpc.InputScriptResp> {
  const message = create(
    signrpc.SignReqSchema,
    request
  );
  const responseBytes = await TurboLnd.signerComputeInputScript(
    toBinary(signrpc.SignReqSchema, message)
  );
  const response = fromBinary(
    signrpc.InputScriptRespSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * SignMessage signs a message with the key specified in the key locator. The
   * returned signature is fixed-size LN wire format encoded.
   *
   * The main difference to SignMessage in the main RPC is that a specific key is
   * used to sign the message instead of the node identity private key.
   *
   * @param [SignMessageReq]
   * @returns [SignMessageResp]
   *
   */
export async function signerSignMessage(
  request: MessageInitShape<typeof signrpc.SignMessageReqSchema>
): Promise<signrpc.SignMessageResp> {
  const message = create(
    signrpc.SignMessageReqSchema,
    request
  );
  const responseBytes = await TurboLnd.signerSignMessage(
    toBinary(signrpc.SignMessageReqSchema, message)
  );
  const response = fromBinary(
    signrpc.SignMessageRespSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * VerifyMessage verifies a signature over a message using the public key
   * provided. The signature must be fixed-size LN wire format encoded.
   *
   * The main difference to VerifyMessage in the main RPC is that the public key
   * used to sign the message does not have to be a node known to the network.
   *
   * @param [VerifyMessageReq]
   * @returns [VerifyMessageResp]
   *
   */
export async function signerVerifyMessage(
  request: MessageInitShape<typeof signrpc.VerifyMessageReqSchema>
): Promise<signrpc.VerifyMessageResp> {
  const message = create(
    signrpc.VerifyMessageReqSchema,
    request
  );
  const responseBytes = await TurboLnd.signerVerifyMessage(
    toBinary(signrpc.VerifyMessageReqSchema, message)
  );
  const response = fromBinary(
    signrpc.VerifyMessageRespSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * DeriveSharedKey returns a shared secret key by performing Diffie-Hellman key
   * derivation between the ephemeral public key in the request and the node's
   * key specified in the key_desc parameter. Either a key locator or a raw
   * public key is expected in the key_desc, if neither is supplied, defaults to
   * the node's identity private key:
   * P_shared = privKeyNode * ephemeralPubkey
   * The resulting shared public key is serialized in the compressed format and
   * hashed with sha256, resulting in the final key length of 256bit.
   *
   * @param [SharedKeyRequest]
   * @returns [SharedKeyResponse]
   *
   */
export async function signerDeriveSharedKey(
  request: MessageInitShape<typeof signrpc.SharedKeyRequestSchema>
): Promise<signrpc.SharedKeyResponse> {
  const message = create(
    signrpc.SharedKeyRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.signerDeriveSharedKey(
    toBinary(signrpc.SharedKeyRequestSchema, message)
  );
  const response = fromBinary(
    signrpc.SharedKeyResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * MuSig2CombineKeys (experimental!) is a stateless helper RPC that can be used
   * to calculate the combined MuSig2 public key from a list of all participating
   * signers' public keys. This RPC is completely stateless and deterministic and
   * does not create any signing session. It can be used to determine the Taproot
   * public key that should be put in an on-chain output once all public keys are
   * known. A signing session is only needed later when that output should be
   * _spent_ again.
   *
   * NOTE: The MuSig2 BIP is not final yet and therefore this API must be
   * considered to be HIGHLY EXPERIMENTAL and subject to change in upcoming
   * releases. Backward compatibility is not guaranteed!
   *
   * @param [MuSig2CombineKeysRequest]
   * @returns [MuSig2CombineKeysResponse]
   *
   */
export async function signerMuSig2CombineKeys(
  request: MessageInitShape<typeof signrpc.MuSig2CombineKeysRequestSchema>
): Promise<signrpc.MuSig2CombineKeysResponse> {
  const message = create(
    signrpc.MuSig2CombineKeysRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.signerMuSig2CombineKeys(
    toBinary(signrpc.MuSig2CombineKeysRequestSchema, message)
  );
  const response = fromBinary(
    signrpc.MuSig2CombineKeysResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * MuSig2CreateSession (experimental!) creates a new MuSig2 signing session
   * using the local key identified by the key locator. The complete list of all
   * public keys of all signing parties must be provided, including the public
   * key of the local signing key. If nonces of other parties are already known,
   * they can be submitted as well to reduce the number of RPC calls necessary
   * later on.
   *
   * NOTE: The MuSig2 BIP is not final yet and therefore this API must be
   * considered to be HIGHLY EXPERIMENTAL and subject to change in upcoming
   * releases. Backward compatibility is not guaranteed!
   *
   * @param [MuSig2SessionRequest]
   * @returns [MuSig2SessionResponse]
   *
   */
export async function signerMuSig2CreateSession(
  request: MessageInitShape<typeof signrpc.MuSig2SessionRequestSchema>
): Promise<signrpc.MuSig2SessionResponse> {
  const message = create(
    signrpc.MuSig2SessionRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.signerMuSig2CreateSession(
    toBinary(signrpc.MuSig2SessionRequestSchema, message)
  );
  const response = fromBinary(
    signrpc.MuSig2SessionResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * MuSig2RegisterNonces (experimental!) registers one or more public nonces of
   * other signing participants for a session identified by its ID. This RPC can
   * be called multiple times until all nonces are registered.
   *
   * NOTE: The MuSig2 BIP is not final yet and therefore this API must be
   * considered to be HIGHLY EXPERIMENTAL and subject to change in upcoming
   * releases. Backward compatibility is not guaranteed!
   *
   * @param [MuSig2RegisterNoncesRequest]
   * @returns [MuSig2RegisterNoncesResponse]
   *
   */
export async function signerMuSig2RegisterNonces(
  request: MessageInitShape<typeof signrpc.MuSig2RegisterNoncesRequestSchema>
): Promise<signrpc.MuSig2RegisterNoncesResponse> {
  const message = create(
    signrpc.MuSig2RegisterNoncesRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.signerMuSig2RegisterNonces(
    toBinary(signrpc.MuSig2RegisterNoncesRequestSchema, message)
  );
  const response = fromBinary(
    signrpc.MuSig2RegisterNoncesResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * MuSig2Sign (experimental!) creates a partial signature using the local
   * signing key that was specified when the session was created. This can only
   * be called when all public nonces of all participants are known and have been
   * registered with the session. If this node isn't responsible for combining
   * all the partial signatures, then the cleanup flag should be set, indicating
   * that the session can be removed from memory once the signature was produced.
   *
   * NOTE: The MuSig2 BIP is not final yet and therefore this API must be
   * considered to be HIGHLY EXPERIMENTAL and subject to change in upcoming
   * releases. Backward compatibility is not guaranteed!
   *
   * @param [MuSig2SignRequest]
   * @returns [MuSig2SignResponse]
   *
   */
export async function signerMuSig2Sign(
  request: MessageInitShape<typeof signrpc.MuSig2SignRequestSchema>
): Promise<signrpc.MuSig2SignResponse> {
  const message = create(
    signrpc.MuSig2SignRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.signerMuSig2Sign(
    toBinary(signrpc.MuSig2SignRequestSchema, message)
  );
  const response = fromBinary(
    signrpc.MuSig2SignResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * MuSig2CombineSig (experimental!) combines the given partial signature(s)
   * with the local one, if it already exists. Once a partial signature of all
   * participants is registered, the final signature will be combined and
   * returned.
   *
   * NOTE: The MuSig2 BIP is not final yet and therefore this API must be
   * considered to be HIGHLY EXPERIMENTAL and subject to change in upcoming
   * releases. Backward compatibility is not guaranteed!
   *
   * @param [MuSig2CombineSigRequest]
   * @returns [MuSig2CombineSigResponse]
   *
   */
export async function signerMuSig2CombineSig(
  request: MessageInitShape<typeof signrpc.MuSig2CombineSigRequestSchema>
): Promise<signrpc.MuSig2CombineSigResponse> {
  const message = create(
    signrpc.MuSig2CombineSigRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.signerMuSig2CombineSig(
    toBinary(signrpc.MuSig2CombineSigRequestSchema, message)
  );
  const response = fromBinary(
    signrpc.MuSig2CombineSigResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * The unique ID of the signing session that should be removed/cleaned up.
   *
   * @param [MuSig2CleanupRequest]
   * @returns [MuSig2CleanupResponse]
   *
   */
export async function signerMuSig2Cleanup(
  request: MessageInitShape<typeof signrpc.MuSig2CleanupRequestSchema>
): Promise<signrpc.MuSig2CleanupResponse> {
  const message = create(
    signrpc.MuSig2CleanupRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.signerMuSig2Cleanup(
    toBinary(signrpc.MuSig2CleanupRequestSchema, message)
  );
  const response = fromBinary(
    signrpc.MuSig2CleanupResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * GetVersion returns the current version and build information of the running
   * daemon.
   *
   * @param [VersionRequest]
   * @returns [Version]
   *
   */
export async function versionerGetVersion(
  request: MessageInitShape<typeof verrpc.VersionRequestSchema>
): Promise<verrpc.Version> {
  const message = create(
    verrpc.VersionRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.versionerGetVersion(
    toBinary(verrpc.VersionRequestSchema, message)
  );
  const response = fromBinary(
    verrpc.VersionSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * ListUnspent returns a list of all utxos spendable by the wallet with a
   * number of confirmations between the specified minimum and maximum. By
   * default, all utxos are listed. To list only the unconfirmed utxos, set
   * the unconfirmed_only to true.
   *
   * @param [ListUnspentRequest]
   * @returns [ListUnspentResponse]
   *
   */
export async function walletKitListUnspent(
  request: MessageInitShape<typeof walletrpc.ListUnspentRequestSchema>
): Promise<walletrpc.ListUnspentResponse> {
  const message = create(
    walletrpc.ListUnspentRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.walletKitListUnspent(
    toBinary(walletrpc.ListUnspentRequestSchema, message)
  );
  const response = fromBinary(
    walletrpc.ListUnspentResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * LeaseOutput locks an output to the given ID, preventing it from being
   * available for any future coin selection attempts. The absolute time of the
   * lock's expiration is returned. The expiration of the lock can be extended by
   * successive invocations of this RPC. Outputs can be unlocked before their
   * expiration through `ReleaseOutput`.
   *
   * @param [LeaseOutputRequest]
   * @returns [LeaseOutputResponse]
   *
   */
export async function walletKitLeaseOutput(
  request: MessageInitShape<typeof walletrpc.LeaseOutputRequestSchema>
): Promise<walletrpc.LeaseOutputResponse> {
  const message = create(
    walletrpc.LeaseOutputRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.walletKitLeaseOutput(
    toBinary(walletrpc.LeaseOutputRequestSchema, message)
  );
  const response = fromBinary(
    walletrpc.LeaseOutputResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * ReleaseOutput unlocks an output, allowing it to be available for coin
   * selection if it remains unspent. The ID should match the one used to
   * originally lock the output.
   *
   * @param [ReleaseOutputRequest]
   * @returns [ReleaseOutputResponse]
   *
   */
export async function walletKitReleaseOutput(
  request: MessageInitShape<typeof walletrpc.ReleaseOutputRequestSchema>
): Promise<walletrpc.ReleaseOutputResponse> {
  const message = create(
    walletrpc.ReleaseOutputRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.walletKitReleaseOutput(
    toBinary(walletrpc.ReleaseOutputRequestSchema, message)
  );
  const response = fromBinary(
    walletrpc.ReleaseOutputResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * ListLeases lists all currently locked utxos.
   *
   * @param [ListLeasesRequest]
   * @returns [ListLeasesResponse]
   *
   */
export async function walletKitListLeases(
  request: MessageInitShape<typeof walletrpc.ListLeasesRequestSchema>
): Promise<walletrpc.ListLeasesResponse> {
  const message = create(
    walletrpc.ListLeasesRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.walletKitListLeases(
    toBinary(walletrpc.ListLeasesRequestSchema, message)
  );
  const response = fromBinary(
    walletrpc.ListLeasesResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * DeriveNextKey attempts to derive the *next* key within the key family
   * (account in BIP43) specified. This method should return the next external
   * child within this branch.
   *
   * @param [KeyReq]
   * @returns [KeyDescriptor]
   *
   */
export async function walletKitDeriveNextKey(
  request: MessageInitShape<typeof walletrpc.KeyReqSchema>
): Promise<signrpc.KeyDescriptor> {
  const message = create(
    walletrpc.KeyReqSchema,
    request
  );
  const responseBytes = await TurboLnd.walletKitDeriveNextKey(
    toBinary(walletrpc.KeyReqSchema, message)
  );
  const response = fromBinary(
    signrpc.KeyDescriptorSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * DeriveKey attempts to derive an arbitrary key specified by the passed
   * KeyLocator.
   *
   * @param [KeyLocator]
   * @returns [KeyDescriptor]
   *
   */
export async function walletKitDeriveKey(
  request: MessageInitShape<typeof signrpc.KeyLocatorSchema>
): Promise<signrpc.KeyDescriptor> {
  const message = create(
    signrpc.KeyLocatorSchema,
    request
  );
  const responseBytes = await TurboLnd.walletKitDeriveKey(
    toBinary(signrpc.KeyLocatorSchema, message)
  );
  const response = fromBinary(
    signrpc.KeyDescriptorSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * NextAddr returns the next unused address within the wallet.
   *
   * @param [AddrRequest]
   * @returns [AddrResponse]
   *
   */
export async function walletKitNextAddr(
  request: MessageInitShape<typeof walletrpc.AddrRequestSchema>
): Promise<walletrpc.AddrResponse> {
  const message = create(
    walletrpc.AddrRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.walletKitNextAddr(
    toBinary(walletrpc.AddrRequestSchema, message)
  );
  const response = fromBinary(
    walletrpc.AddrResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * GetTransaction returns details for a transaction found in the wallet.
   *
   * @param [GetTransactionRequest]
   * @returns [Transaction]
   *
   */
export async function walletKitGetTransaction(
  request: MessageInitShape<typeof walletrpc.GetTransactionRequestSchema>
): Promise<lnrpc.Transaction> {
  const message = create(
    walletrpc.GetTransactionRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.walletKitGetTransaction(
    toBinary(walletrpc.GetTransactionRequestSchema, message)
  );
  const response = fromBinary(
    lnrpc.TransactionSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * ListAccounts retrieves all accounts belonging to the wallet by default. A
   * name and key scope filter can be provided to filter through all of the
   * wallet accounts and return only those matching.
   *
   * @param [ListAccountsRequest]
   * @returns [ListAccountsResponse]
   *
   */
export async function walletKitListAccounts(
  request: MessageInitShape<typeof walletrpc.ListAccountsRequestSchema>
): Promise<walletrpc.ListAccountsResponse> {
  const message = create(
    walletrpc.ListAccountsRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.walletKitListAccounts(
    toBinary(walletrpc.ListAccountsRequestSchema, message)
  );
  const response = fromBinary(
    walletrpc.ListAccountsResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * RequiredReserve returns the minimum amount of satoshis that should be kept
   * in the wallet in order to fee bump anchor channels if necessary. The value
   * scales with the number of public anchor channels but is capped at a maximum.
   *
   * @param [RequiredReserveRequest]
   * @returns [RequiredReserveResponse]
   *
   */
export async function walletKitRequiredReserve(
  request: MessageInitShape<typeof walletrpc.RequiredReserveRequestSchema>
): Promise<walletrpc.RequiredReserveResponse> {
  const message = create(
    walletrpc.RequiredReserveRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.walletKitRequiredReserve(
    toBinary(walletrpc.RequiredReserveRequestSchema, message)
  );
  const response = fromBinary(
    walletrpc.RequiredReserveResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * ListAddresses retrieves all the addresses along with their balance. An
   * account name filter can be provided to filter through all of the
   * wallet accounts and return the addresses of only those matching.
   *
   * @param [ListAddressesRequest]
   * @returns [ListAddressesResponse]
   *
   */
export async function walletKitListAddresses(
  request: MessageInitShape<typeof walletrpc.ListAddressesRequestSchema>
): Promise<walletrpc.ListAddressesResponse> {
  const message = create(
    walletrpc.ListAddressesRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.walletKitListAddresses(
    toBinary(walletrpc.ListAddressesRequestSchema, message)
  );
  const response = fromBinary(
    walletrpc.ListAddressesResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * SignMessageWithAddr returns the compact signature (base64 encoded) created
   * with the private key of the provided address. This requires the address
   * to be solely based on a public key lock (no scripts). Obviously the internal
   * lnd wallet has to possess the private key of the address otherwise
   * an error is returned.
   *
   * This method aims to provide full compatibility with the bitcoin-core and
   * btcd implementation. Bitcoin-core's algorithm is not specified in a
   * BIP and only applicable for legacy addresses. This method enhances the
   * signing for additional address types: P2WKH, NP2WKH, P2TR.
   * For P2TR addresses this represents a special case. ECDSA is used to create
   * a compact signature which makes the public key of the signature recoverable.
   *
   * @param [SignMessageWithAddrRequest]
   * @returns [SignMessageWithAddrResponse]
   *
   */
export async function walletKitSignMessageWithAddr(
  request: MessageInitShape<typeof walletrpc.SignMessageWithAddrRequestSchema>
): Promise<walletrpc.SignMessageWithAddrResponse> {
  const message = create(
    walletrpc.SignMessageWithAddrRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.walletKitSignMessageWithAddr(
    toBinary(walletrpc.SignMessageWithAddrRequestSchema, message)
  );
  const response = fromBinary(
    walletrpc.SignMessageWithAddrResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * VerifyMessageWithAddr returns the validity and the recovered public key of
   * the provided compact signature (base64 encoded). The verification is
   * twofold. First the validity of the signature itself is checked and then
   * it is verified that the recovered public key of the signature equals
   * the public key of the provided address. There is no dependence on the
   * private key of the address therefore also external addresses are allowed
   * to verify signatures.
   * Supported address types are P2PKH, P2WKH, NP2WKH, P2TR.
   *
   * This method is the counterpart of the related signing method
   * (SignMessageWithAddr) and aims to provide full compatibility to
   * bitcoin-core's implementation. Although bitcoin-core/btcd only provide
   * this functionality for legacy addresses this function enhances it to
   * the address types: P2PKH, P2WKH, NP2WKH, P2TR.
   *
   * The verification for P2TR addresses is a special case and requires the
   * ECDSA compact signature to compare the reovered public key to the internal
   * taproot key. The compact ECDSA signature format was used because there
   * are still no known compact signature schemes for schnorr signatures.
   *
   * @param [VerifyMessageWithAddrRequest]
   * @returns [VerifyMessageWithAddrResponse]
   *
   */
export async function walletKitVerifyMessageWithAddr(
  request: MessageInitShape<typeof walletrpc.VerifyMessageWithAddrRequestSchema>
): Promise<walletrpc.VerifyMessageWithAddrResponse> {
  const message = create(
    walletrpc.VerifyMessageWithAddrRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.walletKitVerifyMessageWithAddr(
    toBinary(walletrpc.VerifyMessageWithAddrRequestSchema, message)
  );
  const response = fromBinary(
    walletrpc.VerifyMessageWithAddrResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * ImportAccount imports an account backed by an account extended public key.
   * The master key fingerprint denotes the fingerprint of the root key
   * corresponding to the account public key (also known as the key with
   * derivation path m/). This may be required by some hardware wallets for
   * proper identification and signing.
   *
   * The address type can usually be inferred from the key's version, but may be
   * required for certain keys to map them into the proper scope.
   *
   * For BIP-0044 keys, an address type must be specified as we intend to not
   * support importing BIP-0044 keys into the wallet using the legacy
   * pay-to-pubkey-hash (P2PKH) scheme. A nested witness address type will force
   * the standard BIP-0049 derivation scheme, while a witness address type will
   * force the standard BIP-0084 derivation scheme.
   *
   * For BIP-0049 keys, an address type must also be specified to make a
   * distinction between the standard BIP-0049 address schema (nested witness
   * pubkeys everywhere) and our own BIP-0049Plus address schema (nested pubkeys
   * externally, witness pubkeys internally).
   *
   * NOTE: Events (deposits/spends) for keys derived from an account will only be
   * detected by lnd if they happen after the import. Rescans to detect past
   * events will be supported later on.
   *
   * @param [ImportAccountRequest]
   * @returns [ImportAccountResponse]
   *
   */
export async function walletKitImportAccount(
  request: MessageInitShape<typeof walletrpc.ImportAccountRequestSchema>
): Promise<walletrpc.ImportAccountResponse> {
  const message = create(
    walletrpc.ImportAccountRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.walletKitImportAccount(
    toBinary(walletrpc.ImportAccountRequestSchema, message)
  );
  const response = fromBinary(
    walletrpc.ImportAccountResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * ImportPublicKey imports a public key as watch-only into the wallet. The
   * public key is converted into a simple address of the given type and that
   * address script is watched on chain. For Taproot keys, this will only watch
   * the BIP-0086 style output script. Use ImportTapscript for more advanced key
   * spend or script spend outputs.
   *
   * NOTE: Events (deposits/spends) for a key will only be detected by lnd if
   * they happen after the import. Rescans to detect past events will be
   * supported later on.
   *
   * @param [ImportPublicKeyRequest]
   * @returns [ImportPublicKeyResponse]
   *
   */
export async function walletKitImportPublicKey(
  request: MessageInitShape<typeof walletrpc.ImportPublicKeyRequestSchema>
): Promise<walletrpc.ImportPublicKeyResponse> {
  const message = create(
    walletrpc.ImportPublicKeyRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.walletKitImportPublicKey(
    toBinary(walletrpc.ImportPublicKeyRequestSchema, message)
  );
  const response = fromBinary(
    walletrpc.ImportPublicKeyResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * ImportTapscript imports a Taproot script and internal key and adds the
   * resulting Taproot output key as a watch-only output script into the wallet.
   * For BIP-0086 style Taproot keys (no root hash commitment and no script spend
   * path) use ImportPublicKey.
   *
   * NOTE: Events (deposits/spends) for a key will only be detected by lnd if
   * they happen after the import. Rescans to detect past events will be
   * supported later on.
   *
   * NOTE: Taproot keys imported through this RPC currently _cannot_ be used for
   * funding PSBTs. Only tracking the balance and UTXOs is currently supported.
   *
   * @param [ImportTapscriptRequest]
   * @returns [ImportTapscriptResponse]
   *
   */
export async function walletKitImportTapscript(
  request: MessageInitShape<typeof walletrpc.ImportTapscriptRequestSchema>
): Promise<walletrpc.ImportTapscriptResponse> {
  const message = create(
    walletrpc.ImportTapscriptRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.walletKitImportTapscript(
    toBinary(walletrpc.ImportTapscriptRequestSchema, message)
  );
  const response = fromBinary(
    walletrpc.ImportTapscriptResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * PublishTransaction attempts to publish the passed transaction to the
   * network. Once this returns without an error, the wallet will continually
   * attempt to re-broadcast the transaction on start up, until it enters the
   * chain.
   *
   * @param [Transaction]
   * @returns [PublishResponse]
   *
   */
export async function walletKitPublishTransaction(
  request: MessageInitShape<typeof walletrpc.TransactionSchema>
): Promise<walletrpc.PublishResponse> {
  const message = create(
    walletrpc.TransactionSchema,
    request
  );
  const responseBytes = await TurboLnd.walletKitPublishTransaction(
    toBinary(walletrpc.TransactionSchema, message)
  );
  const response = fromBinary(
    walletrpc.PublishResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * RemoveTransaction attempts to remove the provided transaction from the
   * internal transaction store of the wallet.
   *
   * @param [GetTransactionRequest]
   * @returns [RemoveTransactionResponse]
   *
   */
export async function walletKitRemoveTransaction(
  request: MessageInitShape<typeof walletrpc.GetTransactionRequestSchema>
): Promise<walletrpc.RemoveTransactionResponse> {
  const message = create(
    walletrpc.GetTransactionRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.walletKitRemoveTransaction(
    toBinary(walletrpc.GetTransactionRequestSchema, message)
  );
  const response = fromBinary(
    walletrpc.RemoveTransactionResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * SendOutputs is similar to the existing sendmany call in Bitcoind, and
   * allows the caller to create a transaction that sends to several outputs at
   * once. This is ideal when wanting to batch create a set of transactions.
   *
   * @param [SendOutputsRequest]
   * @returns [SendOutputsResponse]
   *
   */
export async function walletKitSendOutputs(
  request: MessageInitShape<typeof walletrpc.SendOutputsRequestSchema>
): Promise<walletrpc.SendOutputsResponse> {
  const message = create(
    walletrpc.SendOutputsRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.walletKitSendOutputs(
    toBinary(walletrpc.SendOutputsRequestSchema, message)
  );
  const response = fromBinary(
    walletrpc.SendOutputsResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * EstimateFee attempts to query the internal fee estimator of the wallet to
   * determine the fee (in sat/kw) to attach to a transaction in order to
   * achieve the confirmation target.
   *
   * @param [EstimateFeeRequest]
   * @returns [EstimateFeeResponse]
   *
   */
export async function walletKitEstimateFee(
  request: MessageInitShape<typeof walletrpc.EstimateFeeRequestSchema>
): Promise<walletrpc.EstimateFeeResponse> {
  const message = create(
    walletrpc.EstimateFeeRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.walletKitEstimateFee(
    toBinary(walletrpc.EstimateFeeRequestSchema, message)
  );
  const response = fromBinary(
    walletrpc.EstimateFeeResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * PendingSweeps returns lists of on-chain outputs that lnd is currently
   * attempting to sweep within its central batching engine. Outputs with similar
   * fee rates are batched together in order to sweep them within a single
   * transaction.
   *
   * NOTE: Some of the fields within PendingSweepsRequest are not guaranteed to
   * remain supported. This is an advanced API that depends on the internals of
   * the UtxoSweeper, so things may change.
   *
   * @param [PendingSweepsRequest]
   * @returns [PendingSweepsResponse]
   *
   */
export async function walletKitPendingSweeps(
  request: MessageInitShape<typeof walletrpc.PendingSweepsRequestSchema>
): Promise<walletrpc.PendingSweepsResponse> {
  const message = create(
    walletrpc.PendingSweepsRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.walletKitPendingSweeps(
    toBinary(walletrpc.PendingSweepsRequestSchema, message)
  );
  const response = fromBinary(
    walletrpc.PendingSweepsResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * BumpFee is an endpoint that allows users to interact with lnd's sweeper
   * directly. It takes an outpoint from an unconfirmed transaction and sends it
   * to the sweeper for potential fee bumping. Depending on whether the outpoint
   * has been registered in the sweeper (an existing input, e.g., an anchor
   * output) or not (a new input, e.g., an unconfirmed wallet utxo), this will
   * either be an RBF or CPFP attempt.
   *
   * When receiving an input, lnd’s sweeper needs to understand its time
   * sensitivity to make economical fee bumps - internally a fee function is
   * created using the deadline and budget to guide the process. When the
   * deadline is approaching, the fee function will increase the fee rate and
   * perform an RBF.
   *
   * When a force close happens, all the outputs from the force closing
   * transaction will be registered in the sweeper. The sweeper will then handle
   * the creation, publish, and fee bumping of the sweeping transactions.
   * Everytime a new block comes in, unless the sweeping transaction is
   * confirmed, an RBF is attempted. To interfere with this automatic process,
   * users can use BumpFee to specify customized fee rate, budget, deadline, and
   * whether the sweep should happen immediately. It's recommended to call
   * `ListSweeps` to understand the shape of the existing sweeping transaction
   * first - depending on the number of inputs in this transaction, the RBF
   * requirements can be quite different.
   *
   * This RPC also serves useful when wanting to perform a Child-Pays-For-Parent
   * (CPFP), where the child transaction pays for its parent's fee. This can be
   * done by specifying an outpoint within the low fee transaction that is under
   * the control of the wallet.
   *
   * @param [BumpFeeRequest]
   * @returns [BumpFeeResponse]
   *
   */
export async function walletKitBumpFee(
  request: MessageInitShape<typeof walletrpc.BumpFeeRequestSchema>
): Promise<walletrpc.BumpFeeResponse> {
  const message = create(
    walletrpc.BumpFeeRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.walletKitBumpFee(
    toBinary(walletrpc.BumpFeeRequestSchema, message)
  );
  const response = fromBinary(
    walletrpc.BumpFeeResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * ListSweeps returns a list of the sweep transactions our node has produced.
   * Note that these sweeps may not be confirmed yet, as we record sweeps on
   * broadcast, not confirmation.
   *
   * @param [ListSweepsRequest]
   * @returns [ListSweepsResponse]
   *
   */
export async function walletKitListSweeps(
  request: MessageInitShape<typeof walletrpc.ListSweepsRequestSchema>
): Promise<walletrpc.ListSweepsResponse> {
  const message = create(
    walletrpc.ListSweepsRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.walletKitListSweeps(
    toBinary(walletrpc.ListSweepsRequestSchema, message)
  );
  const response = fromBinary(
    walletrpc.ListSweepsResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * LabelTransaction adds a label to a transaction. If the transaction already
   * has a label the call will fail unless the overwrite bool is set. This will
   * overwrite the existing transaction label. Labels must not be empty, and
   * cannot exceed 500 characters.
   *
   * @param [LabelTransactionRequest]
   * @returns [LabelTransactionResponse]
   *
   */
export async function walletKitLabelTransaction(
  request: MessageInitShape<typeof walletrpc.LabelTransactionRequestSchema>
): Promise<walletrpc.LabelTransactionResponse> {
  const message = create(
    walletrpc.LabelTransactionRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.walletKitLabelTransaction(
    toBinary(walletrpc.LabelTransactionRequestSchema, message)
  );
  const response = fromBinary(
    walletrpc.LabelTransactionResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * FundPsbt creates a fully populated PSBT that contains enough inputs to fund
   * the outputs specified in the template. There are three ways a user can
   * specify what we call the template (a list of inputs and outputs to use in
   * the PSBT): Either as a PSBT packet directly with no coin selection (using
   * the legacy "psbt" field), a PSBT with advanced coin selection support (using
   * the new "coin_select" field) or as a raw RPC message (using the "raw"
   * field).
   * The legacy "psbt" and "raw" modes, the following restrictions apply:
   * 1. If there are no inputs specified in the template, coin selection is
   * performed automatically.
   * 2. If the template does contain any inputs, it is assumed that full
   * coin selection happened externally and no additional inputs are added. If
   * the specified inputs aren't enough to fund the outputs with the given fee
   * rate, an error is returned.
   *
   * The new "coin_select" mode does not have these restrictions and allows the
   * user to specify a PSBT with inputs and outputs and still perform coin
   * selection on top of that.
   * For all modes this RPC requires any inputs that are specified to be locked
   * by the user (if they belong to this node in the first place).
   *
   * After either selecting or verifying the inputs, all input UTXOs are locked
   * with an internal app ID.
   *
   * NOTE: If this method returns without an error, it is the caller's
   * responsibility to either spend the locked UTXOs (by finalizing and then
   * publishing the transaction) or to unlock/release the locked UTXOs in case of
   * an error on the caller's side.
   *
   * @param [FundPsbtRequest]
   * @returns [FundPsbtResponse]
   *
   */
export async function walletKitFundPsbt(
  request: MessageInitShape<typeof walletrpc.FundPsbtRequestSchema>
): Promise<walletrpc.FundPsbtResponse> {
  const message = create(
    walletrpc.FundPsbtRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.walletKitFundPsbt(
    toBinary(walletrpc.FundPsbtRequestSchema, message)
  );
  const response = fromBinary(
    walletrpc.FundPsbtResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * SignPsbt expects a partial transaction with all inputs and outputs fully
   * declared and tries to sign all unsigned inputs that have all required fields
   * (UTXO information, BIP32 derivation information, witness or sig scripts)
   * set.
   * If no error is returned, the PSBT is ready to be given to the next signer or
   * to be finalized if lnd was the last signer.
   *
   * NOTE: This RPC only signs inputs (and only those it can sign), it does not
   * perform any other tasks (such as coin selection, UTXO locking or
   * input/output/fee value validation, PSBT finalization). Any input that is
   * incomplete will be skipped.
   *
   * @param [SignPsbtRequest]
   * @returns [SignPsbtResponse]
   *
   */
export async function walletKitSignPsbt(
  request: MessageInitShape<typeof walletrpc.SignPsbtRequestSchema>
): Promise<walletrpc.SignPsbtResponse> {
  const message = create(
    walletrpc.SignPsbtRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.walletKitSignPsbt(
    toBinary(walletrpc.SignPsbtRequestSchema, message)
  );
  const response = fromBinary(
    walletrpc.SignPsbtResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * The name of the account to finalize the PSBT with. If empty, the default
   * wallet account is used.
   *
   * @param [FinalizePsbtRequest]
   * @returns [FinalizePsbtResponse]
   *
   */
export async function walletKitFinalizePsbt(
  request: MessageInitShape<typeof walletrpc.FinalizePsbtRequestSchema>
): Promise<walletrpc.FinalizePsbtResponse> {
  const message = create(
    walletrpc.FinalizePsbtRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.walletKitFinalizePsbt(
    toBinary(walletrpc.FinalizePsbtRequestSchema, message)
  );
  const response = fromBinary(
    walletrpc.FinalizePsbtResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * GetInfo returns general information concerning the companion watchtower
   * including its public key and URIs where the server is currently
   * listening for clients.
   *
   * @param [GetInfoRequest]
   * @returns [GetInfoResponse]
   *
   */
export async function watchtowerGetInfo(
  request: MessageInitShape<typeof watchtowerrpc.GetInfoRequestSchema>
): Promise<watchtowerrpc.GetInfoResponse> {
  const message = create(
    watchtowerrpc.GetInfoRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.watchtowerGetInfo(
    toBinary(watchtowerrpc.GetInfoRequestSchema, message)
  );
  const response = fromBinary(
    watchtowerrpc.GetInfoResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * AddTower adds a new watchtower reachable at the given address and
   * considers it for new sessions. If the watchtower already exists, then
   * any new addresses included will be considered when dialing it for
   * session negotiations and backups.
   *
   * @param [AddTowerRequest]
   * @returns [AddTowerResponse]
   *
   */
export async function watchtowerClientAddTower(
  request: MessageInitShape<typeof wtclientrpc.AddTowerRequestSchema>
): Promise<wtclientrpc.AddTowerResponse> {
  const message = create(
    wtclientrpc.AddTowerRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.watchtowerClientAddTower(
    toBinary(wtclientrpc.AddTowerRequestSchema, message)
  );
  const response = fromBinary(
    wtclientrpc.AddTowerResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * RemoveTower removes a watchtower from being considered for future session
   * negotiations and from being used for any subsequent backups until it's added
   * again. If an address is provided, then this RPC only serves as a way of
   * removing the address from the watchtower instead.
   *
   * @param [RemoveTowerRequest]
   * @returns [RemoveTowerResponse]
   *
   */
export async function watchtowerClientRemoveTower(
  request: MessageInitShape<typeof wtclientrpc.RemoveTowerRequestSchema>
): Promise<wtclientrpc.RemoveTowerResponse> {
  const message = create(
    wtclientrpc.RemoveTowerRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.watchtowerClientRemoveTower(
    toBinary(wtclientrpc.RemoveTowerRequestSchema, message)
  );
  const response = fromBinary(
    wtclientrpc.RemoveTowerResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * DeactivateTower sets the given tower's status to inactive so that it
   * is not considered for session negotiation. Its sessions will also not
   * be used while the tower is inactive.
   *
   * @param [DeactivateTowerRequest]
   * @returns [DeactivateTowerResponse]
   *
   */
export async function watchtowerClientDeactivateTower(
  request: MessageInitShape<typeof wtclientrpc.DeactivateTowerRequestSchema>
): Promise<wtclientrpc.DeactivateTowerResponse> {
  const message = create(
    wtclientrpc.DeactivateTowerRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.watchtowerClientDeactivateTower(
    toBinary(wtclientrpc.DeactivateTowerRequestSchema, message)
  );
  const response = fromBinary(
    wtclientrpc.DeactivateTowerResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * Terminate terminates the given session and marks it as terminal so that
   * it is not used for backups anymore.
   *
   * @param [TerminateSessionRequest]
   * @returns [TerminateSessionResponse]
   *
   */
export async function watchtowerClientTerminateSession(
  request: MessageInitShape<typeof wtclientrpc.TerminateSessionRequestSchema>
): Promise<wtclientrpc.TerminateSessionResponse> {
  const message = create(
    wtclientrpc.TerminateSessionRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.watchtowerClientTerminateSession(
    toBinary(wtclientrpc.TerminateSessionRequestSchema, message)
  );
  const response = fromBinary(
    wtclientrpc.TerminateSessionResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * ListTowers returns the list of watchtowers registered with the client.
   *
   * @param [ListTowersRequest]
   * @returns [ListTowersResponse]
   *
   */
export async function watchtowerClientListTowers(
  request: MessageInitShape<typeof wtclientrpc.ListTowersRequestSchema>
): Promise<wtclientrpc.ListTowersResponse> {
  const message = create(
    wtclientrpc.ListTowersRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.watchtowerClientListTowers(
    toBinary(wtclientrpc.ListTowersRequestSchema, message)
  );
  const response = fromBinary(
    wtclientrpc.ListTowersResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * GetTowerInfo retrieves information for a registered watchtower.
   *
   * @param [GetTowerInfoRequest]
   * @returns [Tower]
   *
   */
export async function watchtowerClientGetTowerInfo(
  request: MessageInitShape<typeof wtclientrpc.GetTowerInfoRequestSchema>
): Promise<wtclientrpc.Tower> {
  const message = create(
    wtclientrpc.GetTowerInfoRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.watchtowerClientGetTowerInfo(
    toBinary(wtclientrpc.GetTowerInfoRequestSchema, message)
  );
  const response = fromBinary(
    wtclientrpc.TowerSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * Stats returns the in-memory statistics of the client since startup.
   *
   * @param [StatsRequest]
   * @returns [StatsResponse]
   *
   */
export async function watchtowerClientStats(
  request: MessageInitShape<typeof wtclientrpc.StatsRequestSchema>
): Promise<wtclientrpc.StatsResponse> {
  const message = create(
    wtclientrpc.StatsRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.watchtowerClientStats(
    toBinary(wtclientrpc.StatsRequestSchema, message)
  );
  const response = fromBinary(
    wtclientrpc.StatsResponseSchema,
    responseBytes
  );
  return response;
}


/**
   *
   * The fee rate, in satoshis per vbyte, that will be used by watchtowers for
   * justice transactions in response to channel breaches.
   *
   * @param [PolicyRequest]
   * @returns [PolicyResponse]
   *
   */
export async function watchtowerClientPolicy(
  request: MessageInitShape<typeof wtclientrpc.PolicyRequestSchema>
): Promise<wtclientrpc.PolicyResponse> {
  const message = create(
    wtclientrpc.PolicyRequestSchema,
    request
  );
  const responseBytes = await TurboLnd.watchtowerClientPolicy(
    toBinary(wtclientrpc.PolicyRequestSchema, message)
  );
  const response = fromBinary(
    wtclientrpc.PolicyResponseSchema,
    responseBytes
  );
  return response;
}

